---
phase: 05-b2b-institution-manager
plan: 09
type: execute
wave: 4
depends_on: [05-01, 05-04, 05-06, 05-07, 05-08]
files_modified:
  - docs/DEPLOY-CHECKLIST.md
autonomous: false
# W-1 note: the `requirements` field below denotes "validated by UAT" — this plan does NOT
# implement INST-05/06/07/08 or EMAIL-03 (implementation owners are 05-01 through 05-08).
# It re-asserts the same IDs because the UAT in Task 2 is the gate that proves they pass
# end-to-end against the live system. Treat this list as an audit/validation manifest, not
# a duplicate ownership claim.
requirements: [INST-05, INST-06, INST-07, INST-08, EMAIL-03]
tags: [docs, deploy-checklist, uat, e2e-smoke, phase-gate]

must_haves:
  truths:
    - "docs/DEPLOY-CHECKLIST.md is updated to include migration 0014 in the migration list (between 0013 and any future entries) AND a new §Phase 5 setup steps section listing: (a) deploy Edge Function Criar-usuario, (b) paste pt-BR email template into Supabase Auth panel per docs/email-templates.md, (c) post-deploy smoke tests for institutional invite + manager dashboard"
    - "End-to-end UAT proves all 5 ROADMAP success criteria for Phase 5: (1) admin creates institution + invites student + assigns manager → manager logs in /gestor while non-managers redirect to /dashboard; (2) manager sees only their own institution's data (RLS-isolated); (3) matrix shows progress per course per student; (4) cert table shows metadata sans download link; (5) institutional invite email contains pt-BR copy mentioning the institution"
    - "ROADMAP.md Phase 5 row updated to 'Complete' with completion date; status updated in REQUIREMENTS.md Traceability table for INST-05/06/07/08 + EMAIL-03"
  artifacts:
    - path: "docs/DEPLOY-CHECKLIST.md"
      provides: "Phase 5 setup additions: 0014 migration + Edge Function deploy + pt-BR template paste"
      contains: "0014_promote_institution_manager_rpc"
  key_links:
    - from: "Operator running pre-prod deploy"
      to: "docs/DEPLOY-CHECKLIST.md §Phase 5 setup steps"
      via: "Manual checklist read-and-execute"
      pattern: "Phase 5"
---

<objective>
Close out Phase 5 by updating the deploy checklist with the new migration + manual setup steps, executing a guided end-to-end UAT that exercises all 5 ROADMAP success criteria, and updating tracking documents (ROADMAP.md + REQUIREMENTS.md Traceability) to reflect the completed phase.

Purpose: Phase 5 has many manual touchpoints (apply 0014 SQL, deploy Edge Function, paste Supabase Auth template) — the deploy checklist must reflect them. The UAT checkpoint proves the complete user journey works in the live system before the phase closes.

Output: 1 modified doc + 1 UAT checkpoint that gates phase completion + 1 tracking-update task.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-b2b-institution-manager/05-CONTEXT.md
@.planning/phases/05-b2b-institution-manager/05-RESEARCH.md
@.planning/phases/05-b2b-institution-manager/05-UI-SPEC.md
@docs/DEPLOY-CHECKLIST.md
@docs/email-templates.md

<interfaces>
<!-- The 5 ROADMAP success criteria for Phase 5 (verbatim from ROADMAP.md lines 104-109): -->

1. Admin cria uma instituição, convida um aluno com role `gestor_instituicao`, e esse aluno consegue logar e acessar `/gestor`; alunos sem esse role são redirecionados para `/dashboard`.
2. Dashboard do gestor em `/gestor[/[slug]]` lista apenas os alunos vinculados a sua propria instituicao — acesso a dados de outra instituicao retorna vazio (RLS, não apenas filtro de aplicação).  (NOTE: D-03 chose `/gestor` singular; documented deviation.)
3. Gestor vê porcentagem de progresso por curso para cada membro da sua equipe.
4. Gestor vê lista de certificados emitidos para membros da sua instituição (nome do aluno, curso, data, código), sem link de download direto do PDF.
5. Convite institucional disparado pelo admin chega com template pt-BR mencionando o nome da instituição contratante.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Update docs/DEPLOY-CHECKLIST.md with Phase 5 additions</name>
  <files>docs/DEPLOY-CHECKLIST.md</files>
  <read_first>
    - docs/DEPLOY-CHECKLIST.md (entire file — find existing §Migrações Pendentes list and §pós-deploy smoke tests section to append to)
    - docs/email-templates.md (the source-of-truth doc — referenced by the new setup step)
    - .planning/phases/05-b2b-institution-manager/05-RESEARCH.md §Common Pitfalls Pitfall 2 (template drift — document the manual re-paste discipline)
  </read_first>
  <action>
Apply 3 surgical additions to `docs/DEPLOY-CHECKLIST.md`. PRESERVE all existing sections.

**Addition 1 — Append to the migration list in §Migrações Pendentes:**

Find the line "13. `0013_institutions_enrollments.sql` — ..." and insert IMMEDIATELY AFTER:

```
14. `0014_promote_institution_manager_rpc.sql` — Phase 5: RPCs SECURITY DEFINER `promote_institution_manager` + `demote_institution_manager` para promote/demote atômico (single transaction, prevent partial-failure "two managers" state). Aplicar isoladamente após 0013.
```

If the warning block about applying 0012/0013 separately is just below, ALSO update the warning text to include 0014:

(append to the existing warning paragraph or add a new line):
```
> ⚠️ **0014:** Após confirmar que 0013 rodou sem erro, abra um novo query e aplique `0014_promote_institution_manager_rpc.sql`. Verifique com `select proname from pg_proc where proname in ('promote_institution_manager','demote_institution_manager');` — devem retornar 2 linhas.
```

**Addition 2 — New section "## Phase 5 — Configuração Manual Adicional":**

Add this section AFTER the migration list and BEFORE any post-deploy smoke test section. If a smoke-test section already exists, place this immediately above it.

```markdown
---

## Phase 5 — Configuração Manual Adicional

Phase 5 adicionou um Edge Function estendido (`Criar-usuario` com suporte a institution_id) e um template pt-BR de email institucional que vive no painel do Supabase Auth (não no repo). Aplicar **uma vez** antes do release de Phase 5 e **revalidar a cada deploy**.

### 1. Deploy do Edge Function `Criar-usuario`

Phase 5 estendeu o Edge Function para aceitar `institution_id` no payload do invite. Após qualquer alteração em `supabase/functions/Criar-usuario/index.ts`:

```bash
supabase functions deploy Criar-usuario
```

Verificação: Supabase Dashboard → **Edge Functions** → `Criar-usuario` → **Logs** mostra evento de deploy recente. Smoke-test invocando a função com payload `institution_id` válido — resposta deve conter `"Convite enviado para {email} da instituição {institutionName}."`

### 2. Template pt-BR no Supabase Auth Panel (EMAIL-03)

**Source of truth:** `docs/email-templates.md`. O template vive no painel do Supabase Auth e **não há API para versionar programaticamente** (verificado em 2026 — Pitfall 2 do RESEARCH Phase 5). Toda alteração em `docs/email-templates.md` exige re-paste manual.

> **W-2 cross-reference:** If you completed plan 05-04 Task 4 (the `[BLOCKING] Deploy Edge Function + paste pt-BR Auth template` operator gate), this re-paste step is just a drift-detection check — confirm the panel still matches `docs/email-templates.md`. Otherwise (if the panel is empty or this is a fresh environment), this is the **first** paste of the pt-BR template and the smoke tests in §6-7 will exercise the institutional pt-BR rendering for the first time.

**Procedimento (uma vez antes do release; repetir a cada alteração no doc):**

1. Abrir `docs/email-templates.md` no editor.
2. Supabase Dashboard → **Authentication** → **Email Templates** → **Invite User**.
3. Copiar §Subject do doc → colar no campo **Subject** do painel.
4. Copiar §HTML Body do doc (bloco `<!DOCTYPE html>...</html>`) → colar no editor de body do painel.
5. Salvar.
6. Smoke-test institucional: enviar convite real de `/admin/instituicoes/[slug]` para email controlado; confirmar:
   - Subject contém o nome da instituição (ex.: "Bem-vindo(a) à plataforma MDHE — convite de Colégio X")
   - Body em pt-BR contém "como aluno(a) da Colégio X"
   - Botão "Aceitar convite e criar minha senha" presente; link aponta para URL de confirmação Supabase
7. Smoke-test fallback B2C: convidar via `/admin/usuarios` (sem institution_id); confirmar body usa branch `{{ else }}` ("Você foi convidado(a) pela MDHE Consultoria...") sem nome de instituição.

**Detecção de drift:** antes de cada deploy de produção, comparar visualmente o template no painel com `docs/email-templates.md`. Se divergirem, re-aplicar do doc.

> **Operator warning (I-1):** O Subject do template tem o limite documentado em `docs/email-templates.md` §Known limitations — para convites B2C legados (sem `institution_name`), o Subject resolve para `"Bem-vindo(a) à plataforma MDHE — convite de "` com **espaço final** (porque Subject não suporta `{{ if }}` Go-template). Esta "feiura" cosmética é aceitável para v1 (apenas o admin invoca o caminho B2C via `/admin/usuarios`); só requer ação se um polish pre-release for solicitado.

### 3. Smoke-test Phase 5 pós-deploy

Executar como conta admin **após** todos os passos acima estarem aplicados:

- [ ] `/admin/instituicoes` carrega sem erro; mostra "Nenhuma instituição cadastrada" se for o primeiro deploy
- [ ] Criar instituição teste via `/admin/instituicoes/nova` redireciona para `/admin/instituicoes/[slug]` após sucesso
- [ ] Invite institucional via aba "Convidar novo aluno" envia email pt-BR com nome da instituição (ver §2 acima)
- [ ] Aluno aceita convite, faz login, é redirecionado para `/dashboard` (role `student` por default)
- [ ] Admin promove aluno a gestor via "Promover a gestor" → toast de sucesso → aluno faz login e acessa `/gestor` com sucesso
- [ ] Aluno sem role `institution_manager` tentando acessar `/gestor` é redirecionado para `/dashboard`
- [ ] Admin tentando acessar `/gestor` é redirecionado para `/admin/instituicoes`
- [ ] Cleanup: `delete from institution_members where institution_id = '<test_id>'; delete from institutions where slug = '<test_slug>'; delete from auth.users where email = '<test_email>';`
```

**Addition 3 — If the file has a "Last updated" footer line, update the date and append "Phase 5" to the change log line:**

```
*Last updated: {YYYY-MM-DD} after Phase 5 (B2B Institution Manager) completion*
```

Run `git diff docs/DEPLOY-CHECKLIST.md` to confirm only the migration list bullet 14, the warning addition, the new Phase 5 section, and the footer date are modified.
  </action>
  <verify>
    <automated>grep -q "0014_promote_institution_manager_rpc" docs/DEPLOY-CHECKLIST.md &amp;&amp; grep -q "Phase 5 — Configuração Manual" docs/DEPLOY-CHECKLIST.md &amp;&amp; grep -q "supabase functions deploy Criar-usuario" docs/DEPLOY-CHECKLIST.md &amp;&amp; grep -q "Source of truth" docs/DEPLOY-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - File `docs/DEPLOY-CHECKLIST.md` contains entry `14. 0014_promote_institution_manager_rpc.sql` in the migration list
    - File contains a new section header `## Phase 5 — Configuração Manual Adicional`
    - Section contains the literal command `supabase functions deploy Criar-usuario`
    - Section references `docs/email-templates.md` and the phrase "Source of truth"
    - Section contains a smoke-test checklist with at least 6 items covering: list page loads, create institution, invite institutional, role gating (admin vs manager vs student), and cleanup SQL
    - The existing migration list (1-13) and existing sections are unchanged (verify via git diff)
    - File still passes any markdown lint if configured (none in this repo per CLAUDE.md, but ensure no broken syntax)
  </acceptance_criteria>
  <done>
DEPLOY-CHECKLIST.md updated with 0014 migration + Phase 5 manual setup steps + post-deploy smoke checklist. Pitfall 2 drift discipline documented inline.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: [BLOCKING UAT] End-to-end Phase 5 verification covering all 5 ROADMAP success criteria</name>
  <what-built>
The full Phase 5 surface:
- Migration 0014 applied (plan 05-01)
- Test scaffolds + production institutions module + server actions + middleware updates (plans 05-02 through 05-06)
- Edge Function extended + pt-BR email template ready in Supabase Auth panel (plan 05-04, applied via 05-04 Task 4 checkpoint)
- Admin UI at `/admin/instituicoes/*` (plan 05-07)
- Manager dashboard at `/gestor` (plan 05-08)
- Deploy checklist updated (Task 1 of this plan)

Now run a guided end-to-end test that exercises ALL 5 ROADMAP success criteria using REAL accounts and REAL data in the live (or staging) Supabase project. Plans 05-01 and 05-04 have their own operator gates; this UAT proves the complete journey end-to-end.
  </what-built>
  <how-to-verify>
**Pre-conditions:**
- All plans 05-01 through 05-08 are complete; CI is green
- Plans 05-01 and 05-04 operator-blocking checkpoints PASSED (migration applied, Edge Function deployed, pt-BR template pasted)
- You have:
  - An admin account
  - 2 personal email accounts you control (e.g., `you+inst-a@gmail.com`, `you+inst-b@gmail.com`)

**UAT Procedure — execute in order, mark each step PASS or FAIL:**

### Criterion 1: Admin creates institution + invites student + assigns manager; gestor logs in /gestor; non-manager redirected to /dashboard

1. Log in as admin → navigate to `/dashboard`. Confirm the new "Gerenciar instituições" pill in the admin nav block (added in 05-06).
2. Click "Gerenciar instituições" → land on `/admin/instituicoes`. Confirm pt-BR copy: "Instituições contratantes", "Nova instituição".
3. Click "Nova instituição" → fill form (Nome: "Phase 5 UAT Inst A", let slug auto-fill). Submit.
4. Land on `/admin/instituicoes/phase-5-uat-inst-a`. Confirm breadcrumb shows "Instituições > Phase 5 UAT Inst A".
5. Click "Convidar novo aluno" tab. Fill name + your `you+inst-a@gmail.com` email. Submit.
6. Confirm success banner: "Convite enviado para you+inst-a@gmail.com da instituição Phase 5 UAT Inst A."
7. Open the invited email in your inbox. **(Verifies Criterion 5 below — capture inline now.)**
8. Click invite link → set password → land on `/dashboard` (student role default). Logout.
9. As admin, return to `/admin/instituicoes/phase-5-uat-inst-a`. Confirm the new aluno appears in the members list with badge "Aluno".
10. Click "Promover a gestor" on that row. Since this is the first manager, NO confirmation dialog should appear (per UI-SPEC §Promover hierarchy: no-prior-manager → direct submit). Confirm success banner.
11. Logout admin → log in as the newly-promoted aluno (using `you+inst-a@gmail.com` account).
12. After login, confirm immediate redirect to `/gestor` is NOT automatic (login lands at `/dashboard`); navigate to `/gestor` manually.
13. Confirm `/gestor` loads with hero card showing "Phase 5 UAT Inst A".
14. Logout. Log in as a different non-manager student account. Navigate to `/gestor`. Confirm redirect to `/dashboard`.
15. Logout. Log in as admin. Navigate to `/gestor`. Confirm redirect to `/admin/instituicoes` (per D-02).

**Criterion 1 PASS / FAIL:** _____________

### Criterion 2: Manager sees only their own institution's data (RLS-validated, not just app filter)

16. As admin, create a SECOND institution: "Phase 5 UAT Inst B" with email `you+inst-b@gmail.com`.
17. Invite a student via institution B's detail page. Accept the invite. Promote that student to gestor.
18. Log in as gestor of Inst A (`you+inst-a@gmail.com`). At `/gestor`, confirm the matrix and certs sections only show Inst A members (no Inst B data).
19. **RLS isolation proof:** open Supabase Dashboard → SQL Editor → run as the gestor of Inst A's session (use Auth Admin Token impersonation or set `request.jwt.claim.role` via the SQL Editor authenticated mode). Run:
    ```sql
    -- Replace <inst_b_id> with the actual UUID of Inst B
    select count(*) from institution_members where institution_id = '<inst_b_id>';
    ```
    Expected: `0` (RLS policy "Members read own membership" filters to the gestor's own institution_id; gestor cannot see Inst B membership).
20. Repeat the same SQL with `enrollments` filter `institution_id = '<inst_b_id>'`. Expected: `0`.

**Criterion 2 PASS / FAIL:** _____________

### Criterion 3: Manager sees percentage progress per course per member

21. As admin, grant the Inst A gestor's student-account access to a published course (use existing `/admin/cursos/[slug]/alunos` flow). Mark a few lessons as completed (login as that student first, complete some lessons, logout).
22. Log in as Inst A gestor → /gestor. Confirm the matrix has at least one row (the student) and at least one column (the course). Confirm the cell shows "{N}% · {x}/{y} aulas" with sensible numbers.
23. Mark one more lesson complete as the student. Refresh /gestor. Confirm the percentage updates.

**Criterion 3 PASS / FAIL:** _____________

### Criterion 4: Manager sees certificates with metadata (no direct download)

24. As the student of Inst A, complete 100% of the lessons in the course. Confirm the cert is auto-issued (Phase 3 behavior).
25. Log in as Inst A gestor → /gestor. Scroll to "Certificados emitidos" section. Confirm a row appears with: aluno name, course title, date in pt-BR (DD/MM/YYYY in America/Sao_Paulo), and certificate code in monospace.
26. Confirm the certificate code text is `select-all`-able (single click selects the entire UUID).
27. Confirm there is NO download link, NO PDF preview button, NO `<a href="...pdf">` (D-15: read-only metadata).

**Criterion 4 PASS / FAIL:** _____________

### Criterion 5: Institutional invite email is pt-BR with institution name (already partially verified in step 7)

28. From step 7's email: confirm Subject contains "Bem-vindo(a) à plataforma MDHE — convite de Phase 5 UAT Inst A".
29. Confirm body contains literal: "Você foi convidado(a) pela MDHE Consultoria para acessar a plataforma de cursos como aluno(a) da Phase 5 UAT Inst A."
30. Confirm "Aceitar convite e criar minha senha" button works.

**Criterion 5 PASS / FAIL:** _____________

### Bonus checks (orphan + auto-demote + soft-detach):

31. **Orphan flow:** As admin, run `delete from institution_members where institution_id = '<inst_a_id>' and profile_id = '<inst_a_gestor_id>';` (manually orphans the gestor — leaves `profiles.role = 'institution_manager'` but no membership row). Log in as the orphan gestor → navigate to `/gestor`. Confirm redirect to `/dashboard?notice=orphan-manager` AND the amber banner "Sua instituição ainda não foi configurada. Entre em contato com a MDHE." renders.
32. **Auto-demote (D-07):** As admin, in Inst A, attach a new student. Click "Promover a gestor" on the new student. Since a gestor already exists, ConfirmationDialog should appear with "Auto-demote disclosure". Confirm. After action, verify (a) old gestor's badge changed to "Aluno"; (b) old gestor's `profiles.role` flipped back to "student" (check via SQL: `select role from profiles where id = '<old_gestor_id>';`); (c) only one row in `institution_members` for Inst A has `role = 'manager'`.
33. **Soft detach (D-08):** Detach a member who has enrollments. Confirm: (a) `institution_members` row is gone; (b) `enrollments` row STILL EXISTS with `institution_id` unchanged; (c) `course_certificates` row (if any) STILL EXISTS.

**Bonus PASS / FAIL:** _____________

---

### Cleanup (run after all criteria PASS):

```sql
delete from institution_members where institution_id in (select id from institutions where slug like 'phase-5-uat%');
delete from enrollments where institution_id in (select id from institutions where slug like 'phase-5-uat%');
delete from course_certificates where user_id in (select id from auth.users where email like 'you+inst-%@gmail.com');
delete from institutions where slug like 'phase-5-uat%';
delete from auth.users where email like 'you+inst-%@gmail.com';
```
  </how-to-verify>
  <resume-signal>
Type "uat-pass" if all 5 criteria + bonus checks PASS. If any FAIL, paste the failing step number + observed behavior + screenshot/error message. Plan 05-09 Task 3 (tracking updates) only proceeds after UAT passes.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update ROADMAP.md + REQUIREMENTS.md tracking after UAT passes</name>
  <files>.planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/ROADMAP.md (locate Phase 5 row in §Progress table + Phase 5 detail block + plan checklist)
    - .planning/REQUIREMENTS.md (locate Traceability table for INST-05/06/07/08 + EMAIL-03)
  </read_first>
  <action>
**Sub-step A — Update `.planning/ROADMAP.md`:**

1. In the §Phases overview list (top), change:
   ```
   - [ ] **Phase 5: B2B Institution Manager** - Institution schema TypeScript layer, /gestor dashboard, invite emails via Resend
   ```
   to:
   ```
   - [x] **Phase 5: B2B Institution Manager** - Institution schema TypeScript layer, /gestor dashboard, invite emails via Resend (completed {YYYY-MM-DD})
   ```
   (Use today's date in YYYY-MM-DD format.)

2. In the Phase 5 detail block, change `**Plans**: TBD` to `**Plans**: 9 plans` and append the 9 plan entries (or update the existing list if /gsd-plan-phase added them):
   ```
   Plans:
   - [x] 05-01-PLAN.md — Migration 0014 (promote/demote RPCs SECURITY DEFINER) + types regen + README + [BLOCKING] SQL apply
   - [x] 05-02-PLAN.md — Wave 0 test scaffolds (8 test files with it.todo placeholders)
   - [x] 05-03-PLAN.md — src/lib/institutions/{types,schema,queries}.ts + tests
   - [x] 05-04-PLAN.md — Edge Function institution_id branch + browser wrapper + docs/email-templates.md + [BLOCKING] deploy + Auth panel paste
   - [x] 05-05-PLAN.md — 5 Server Actions (upsert/attach/promote/demote/detach/search) + tests
   - [x] 05-06-PLAN.md — Middleware GESTOR ring + dashboard orphan-manager flash + admin nav link + middleware tests
   - [x] 05-07-PLAN.md — Admin UI /admin/instituicoes/* (4 RSC + 3 client + MemberRoleBadge)
   - [x] 05-08-PLAN.md — Manager dashboard /gestor (layout + page + ProgressMatrix + certs + MdheContactCard)
   - [x] 05-09-PLAN.md — DEPLOY-CHECKLIST.md update + UAT checkpoint + tracking updates
   ```

3. In the §Progress table at the bottom of ROADMAP.md, change the Phase 5 row from:
   ```
   | 5. B2B Institution Manager | 0/TBD | Not started | - |
   ```
   to:
   ```
   | 5. B2B Institution Manager | 9/9 | Complete    | {YYYY-MM-DD} |
   ```

**Sub-step B — Update `.planning/REQUIREMENTS.md`:**

In the Traceability table (lines ~174-183), change the Status column for these 5 rows from "Pending" to "Complete":
- INST-05 → Complete
- INST-06 → Complete
- INST-07 → Complete
- INST-08 → Complete
- EMAIL-03 → Complete

Also flip the `[ ]` to `[x]` in the v1 Requirements section for the same 5 IDs (e.g., `- [x] **INST-05**: ...`).

Do NOT touch other rows (CAT-*, ENR-*, PROG-*, CERT-*, MKT-*, EMAIL-01/02 are owned by other phases and stay as they were).

**Sub-step C — Verify:**

```bash
grep -E "Phase 5: B2B" .planning/ROADMAP.md  # confirm [x] checkbox
grep "INST-05" .planning/REQUIREMENTS.md  # confirm both [x] in v1 list and Complete in Traceability
```

After all edits, the docs reflect Phase 5 closure.
  </action>
  <verify>
    <automated>grep -q "\\[x\\] \\*\\*Phase 5: B2B Institution Manager" .planning/ROADMAP.md &amp;&amp; grep -q "9/9 | Complete" .planning/ROADMAP.md &amp;&amp; grep -E "INST-05.*Phase 5.*Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -E "INST-08.*Phase 5.*Complete" .planning/REQUIREMENTS.md &amp;&amp; grep -E "EMAIL-03.*Phase 5.*Complete" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - .planning/ROADMAP.md Phase 5 entry has `[x]` checkbox AND completion date
    - .planning/ROADMAP.md §Progress table Phase 5 row reads "9/9 | Complete | {YYYY-MM-DD}"
    - .planning/REQUIREMENTS.md Traceability rows for INST-05, INST-06, INST-07, INST-08, EMAIL-03 all read "Complete" in the Status column
    - .planning/REQUIREMENTS.md v1 Requirements section has `[x]` for the same 5 IDs
    - No other phase entries or requirement IDs were modified (verify with git diff)
  </acceptance_criteria>
  <done>
ROADMAP + REQUIREMENTS reflect Phase 5 closure. Tracking docs are accurate; v1 milestone has 5 more requirements moved from Pending to Complete.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan is doc + UAT only. The UAT exercises the production system but does not introduce new code. The deploy checklist is operational doc.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09-01 | Tampering | UAT runner skips a step and the docs are marked complete | mitigate | Each criterion has explicit PASS/FAIL inline; bonus checks include orphan + auto-demote + soft-detach (the 3 most-likely-to-regress areas). Cleanup SQL ensures the test data doesn't pollute production. |
| T-05-09-02 | Information Disclosure | UAT screenshots leak personal email addresses | accept | Use disposable email aliases (`you+...@gmail.com`); cleanup SQL removes data after pass. |
| T-05-09-03 | Repudiation | Phase marked complete without operator-blocking checkpoint passing | mitigate | Task 2 is `checkpoint:human-verify gate="blocking"`. Task 3 (tracking updates) lives downstream and only runs after UAT signal "uat-pass". |
| T-05-09-04 | Tampering | Operator runs cleanup SQL on production data with broader pattern than intended | mitigate | Cleanup uses `where slug like 'phase-5-uat%'` — namespaced to UAT institutions. Email cleanup uses `like 'you+inst-%@gmail.com'` — narrow pattern. Operator can dry-run with `select` first. |
</threat_model>

<verification>
- [ ] docs/DEPLOY-CHECKLIST.md updated (Task 1 acceptance)
- [ ] UAT passes all 5 ROADMAP success criteria + bonus (Task 2 operator signal "uat-pass")
- [ ] ROADMAP.md + REQUIREMENTS.md tracking reflect Phase 5 closure (Task 3 acceptance)
- [ ] `npm run lint && npm run typecheck && npm run test:ci && npm run build` all green at phase end
- [ ] Cleanup SQL run; no UAT artifacts remain in production
</verification>

<success_criteria>
- All 5 ROADMAP Phase 5 success criteria validated by live E2E test (UAT)
- Bonus: orphan-manager flow works, auto-demote (D-07) works, soft-detach (D-08) works
- DEPLOY-CHECKLIST.md is the canonical pre-prod doc for the operator
- Tracking docs (ROADMAP, REQUIREMENTS) reflect 5 more requirements moved from Pending to Complete
- Phase 5 is shippable: admin can create institutions, invite students with pt-BR email, assign managers; managers see their team's progress + certs; non-managers cannot reach /gestor
</success_criteria>

<output>
After completion, create `.planning/phases/05-b2b-institution-manager/05-09-SUMMARY.md` capturing:
- DEPLOY-CHECKLIST.md diff summary
- UAT results matrix: 5 criteria + 3 bonus checks all PASS
- ROADMAP + REQUIREMENTS update diff summary
- Phase 5 final note: completion date, closing handoff for /gsd-transition or /gsd-complete-milestone (v1 milestone now has Phase 5 done; only Phase 2 remains pending in roadmap)
</output>
