---
phase: 05-b2b-institution-manager
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - src/lib/institutions/queries.test.ts
  - src/lib/institutions/schema.test.ts
  - src/app/actions/upsert-institution.test.ts
  - src/app/actions/attach-institution-member.test.ts
  - src/app/actions/promote-institution-manager.test.ts
  - src/app/actions/detach-institution-member.test.ts
  - src/app/actions/search-students-for-institution.test.ts
  - middleware.test.ts
autonomous: true
requirements: [INST-05, INST-06, INST-07, INST-08]
tags: [vitest, test-scaffold, wave-0, nyquist, tdd-skeleton]

must_haves:
  truths:
    - "Every Phase 5 implementation file has a paired test file containing at minimum a `describe(...)` block and one or more `it.todo(...)` (or skipped failing) tests that name the behavior being asserted"
    - "Running `npm run test:ci` does NOT regress (existing 38 tests still pass; new test files declare todos but do not fail)"
    - "Each test file imports the production module it targets so that when the production module is created in subsequent waves, only the test bodies need filling — the wiring is in place"
    - "Test files cover INST-05 (middleware), INST-06 (queries isolation), INST-07 (cert ordering + matrix), INST-08 (server actions)"
  artifacts:
    - path: "src/lib/institutions/queries.test.ts"
      provides: "Test scaffold for getInstitutionForManager / getInstitutionMembersWithProgress / getInstitutionCertificates"
      contains: "describe(\"getInstitutionMembersWithProgress\""
    - path: "src/lib/institutions/schema.test.ts"
      provides: "Test scaffold for Zod schemas"
      contains: "describe(\"createInstitutionSchema\""
    - path: "src/app/actions/upsert-institution.test.ts"
      provides: "Test scaffold for createInstitutionAction"
      contains: "describe(\"createInstitutionAction\""
    - path: "src/app/actions/attach-institution-member.test.ts"
      provides: "Test scaffold for attachInstitutionMemberAction"
      contains: "describe(\"attachInstitutionMemberAction\""
    - path: "src/app/actions/promote-institution-manager.test.ts"
      provides: "Test scaffold for promoteInstitutionManagerAction"
      contains: "describe(\"promoteInstitutionManagerAction\""
    - path: "src/app/actions/detach-institution-member.test.ts"
      provides: "Test scaffold for detachInstitutionMemberAction"
      contains: "describe(\"detachInstitutionMemberAction\""
    - path: "src/app/actions/search-students-for-institution.test.ts"
      provides: "Test scaffold for searchStudentsForInstitution"
      contains: "describe(\"searchStudentsForInstitution\""
    - path: "middleware.test.ts"
      provides: "Test scaffold for /gestor route gating logic in middleware.ts"
      contains: "describe(\"middleware /gestor gate\""
  key_links:
    - from: "Each Wave 1+ implementation task's <verify><automated> command"
      to: "Test files created in this plan"
      via: "Imports + it.todo placeholders ready to convert to passing tests"
      pattern: "npx vitest run .*test\\.ts"
---

<objective>
Create test-file skeletons for every Phase 5 module that has an `<automated>` verify command in plans 05-03 through 05-08. Each scaffold contains the boilerplate (`vi.mock` block, `describe` block, named `it.todo` placeholders) so subsequent waves' implementations can fill in test bodies and immediately pass automated verification — satisfying the Nyquist sampling rule (every code-producing task must have an automated verify, and the test file must exist by the time the implementation lands).

Purpose: Per VALIDATION.md Wave 0 Requirements, eight test files do not exist today and would otherwise force every implementation task to declare `MISSING — Wave 0 must create {file} first`. Pre-creating the scaffolds removes that friction and aligns with TDD discipline (test names are written first, asserting the behavior in the names themselves).

Output: Eight test files, each importing its target module via `@/...` aliases (which will fail at module-resolution time because the production files don't exist yet — this is intentional and tests are wrapped in `it.todo()` to skip execution until the module exists). When Wave 1 ships the production modules, the executor swaps `it.todo` for `it`/`describe` bodies and verifies green.

**Self-check:** This plan does NOT implement any production code. It writes only test files containing `it.todo` placeholders + the standard `vi.mock` boilerplate from `grant-enrollment.test.ts:1-46` (the canonical analog).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-b2b-institution-manager/05-CONTEXT.md
@.planning/phases/05-b2b-institution-manager/05-RESEARCH.md
@.planning/phases/05-b2b-institution-manager/05-PATTERNS.md
@.planning/phases/05-b2b-institution-manager/05-VALIDATION.md
@CLAUDE.md
@src/app/actions/grant-enrollment.test.ts
@vitest.config.ts

<interfaces>
<!-- The grant-enrollment.test.ts file at src/app/actions/grant-enrollment.test.ts is the
     CANONICAL pattern. Copy its top-of-file vi.mock block verbatim for every new test file. -->

From src/app/actions/grant-enrollment.test.ts (top-of-file mock setup, lines 1-46):
```typescript
import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("next/navigation", () => ({
  redirect: vi.fn((url: string) => { throw new Error(`NEXT_REDIRECT:${url}`); }),
}));
vi.mock("@/lib/auth/roles", () => ({ fetchUserRole: vi.fn() }));
vi.mock("@/lib/supabase/server", () => ({ createSupabaseServerClient: vi.fn() }));
vi.mock("@/lib/supabase/admin", () => ({ createSupabaseAdminClient: vi.fn() }));
vi.mock("@sentry/nextjs", () => ({ captureException: vi.fn() }));
vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }));
vi.mock("@/lib/logger", () => ({
  logger: { debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn() },
}));
```

From vitest.config.ts: `environment: "node"`, alias `@` → `src`, includes `src/**/*.test.{ts,tsx}` AND root-level files (verify by reading vitest.config.ts during execution).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create the 5 server-action test scaffolds (upsert/attach/promote/detach/search)</name>
  <files>src/app/actions/upsert-institution.test.ts, src/app/actions/attach-institution-member.test.ts, src/app/actions/promote-institution-manager.test.ts, src/app/actions/detach-institution-member.test.ts, src/app/actions/search-students-for-institution.test.ts</files>
  <read_first>
    - src/app/actions/grant-enrollment.test.ts (lines 1-46 — vi.mock block; lines 47-95 — makeServerSupabase / makeAdminClientChain factories; lines 100-200 — at least 2 `it(...)` examples)
    - .planning/phases/05-b2b-institution-manager/05-PATTERNS.md §Test files for actions and queries (lines ~1122-1188)
    - .planning/phases/05-b2b-institution-manager/05-RESEARCH.md §Code Examples §Example 5 (lines 1141-1183 — full promote test)
    - .planning/phases/05-b2b-institution-manager/05-VALIDATION.md §Wave 0 Requirements (the full list of test todos to enumerate per file)
  </read_first>
  <action>
Create FIVE test files under `src/app/actions/`. Each file uses the SAME top-of-file boilerplate (the `vi.mock` block from grant-enrollment.test.ts:1-46, copied verbatim). Each file imports the module-under-test using `import { … } from "./<module-name>"`. These imports will fail at runtime because the production modules don't exist yet — wrap every test in `it.todo(...)` so vitest treats them as pending placeholders and does NOT attempt to import the missing module.

**Important Vitest semantics:** `it.todo(name)` records a pending test without executing the test body or evaluating its closure. To prevent the file's TOP-LEVEL `import { thingX } from "./missing-module"` from blowing up, wrap each module-under-test import in a `// @ts-expect-error - production module created in 05-XX` line and use a try/catch or — simpler — DO NOT import the production module at all in scaffold mode. Instead, declare the imports as comments showing what they will become, and reference function names via string literals in `it.todo(...)`. When the implementation lands, the executor uncomments the import and replaces `it.todo` with `it`.

**Concrete file template (use for every one of the 5 files; only the `describe` name and todo list change):**

```typescript
// src/app/actions/upsert-institution.test.ts
//
// Wave 0 scaffold for INST-08: createInstitutionAction + updateInstitutionAction.
// Production module created in plan 05-05; until then these are it.todo placeholders.
//
// Pattern source: src/app/actions/grant-enrollment.test.ts (lines 1-46 vi.mock block,
// lines 47-95 mock factories — replicate verbatim when filling in test bodies).

import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("next/navigation", () => ({
  redirect: vi.fn((url: string) => { throw new Error(`NEXT_REDIRECT:${url}`); }),
}));
vi.mock("@/lib/auth/roles", () => ({ fetchUserRole: vi.fn() }));
vi.mock("@/lib/supabase/server", () => ({ createSupabaseServerClient: vi.fn() }));
vi.mock("@/lib/supabase/admin", () => ({ createSupabaseAdminClient: vi.fn() }));
vi.mock("@sentry/nextjs", () => ({ captureException: vi.fn() }));
vi.mock("next/cache", () => ({ revalidatePath: vi.fn() }));
vi.mock("@/lib/logger", () => ({
  logger: { debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn() },
}));

// Imports of the production module are DELIBERATELY omitted in Wave 0.
// Plan 05-05 will add: import { createInstitutionAction } from "./upsert-institution";

describe("createInstitutionAction", () => {
  beforeEach(() => vi.clearAllMocks());

  it.todo("rejects non-admin callers with errorMessage 'Voce nao tem permissao'");
  it.todo("rejects payload with empty name (Zod fieldErrors.name set)");
  it.todo("rejects payload with invalid slug (regex violation)");
  it.todo("returns success with redirect path /admin/instituicoes/{slug} on happy path");
  it.todo("translates Postgres 23505 (unique violation) to 'Já existe uma instituição com este slug.'");
});

describe("updateInstitutionAction", () => {
  beforeEach(() => vi.clearAllMocks());

  it.todo("rejects non-admin callers");
  it.todo("requires institutionId in payload");
  it.todo("returns success on happy update path");
});

// Use expect to silence "no expect calls" warning if Vitest enforces it; harmless no-op.
it("scaffold sentinel — replace when production module exists", () => {
  expect(true).toBe(true);
});
```

**Per-file contents (file → describe blocks → it.todo names):**

**File 1 — `src/app/actions/upsert-institution.test.ts`** (template above, verbatim).

**File 2 — `src/app/actions/attach-institution-member.test.ts`**:
- `describe("attachInstitutionMemberAction", ...)` with todos:
  - "rejects non-admin callers with errorMessage 'Voce nao tem permissao'"
  - "rejects missing institution_id (Zod failure)"
  - "rejects missing profile_id (Zod failure)"
  - "calls institution_members.upsert with onConflict 'institution_id,profile_id'"
  - "returns success message on happy path"
  - "translates 23505 unique violation to friendly 'Aluno já vinculado'"

**File 3 — `src/app/actions/promote-institution-manager.test.ts`**:
- `describe("promoteInstitutionManagerAction", ...)` with todos:
  - "rejects non-admin callers"
  - "calls promote_institution_manager RPC with p_institution_id and p_new_manager_profile_id"
  - "returns success message on happy path"
  - "captures Sentry exception when RPC returns error"
  - "calls revalidatePath('/admin/instituicoes/{slug}')"
- `describe("demoteInstitutionManagerAction", ...)` with todos:
  - "rejects non-admin callers"
  - "calls demote_institution_manager RPC with p_institution_id and p_profile_id"
  - "returns success message on happy path"

**File 4 — `src/app/actions/detach-institution-member.test.ts`**:
- `describe("detachInstitutionMemberAction", ...)` with todos:
  - "rejects non-admin callers"
  - "calls institution_members.delete().eq('institution_id', ...).eq('profile_id', ...)"
  - "does NOT touch enrollments table (D-08: soft detach)"
  - "returns success message on happy path"

**File 5 — `src/app/actions/search-students-for-institution.test.ts`**:
- `describe("searchStudentsForInstitution", ...)` with todos:
  - "returns [] for non-admin caller"
  - "returns [] for query length < 2"
  - "excludes profile_ids already in institution_members"
  - "filters by full_name OR email (ilike) and limits to 20 results"
  - "returns shape { id, fullName, email }"

For all 5 files, keep the `// scaffold sentinel — replace when production module exists` test at the bottom as the single executable test. This satisfies vitest's "no test in file" warning and gives `npm run test:ci` something concrete to count.
  </action>
  <verify>
    <automated>npm run test:ci 2>&amp;1 | grep -E "(scaffold sentinel|upsert-institution|attach-institution-member|promote-institution-manager|detach-institution-member|search-students-for-institution)" | head -20 &amp;&amp; npx vitest run src/app/actions/upsert-institution.test.ts src/app/actions/attach-institution-member.test.ts src/app/actions/promote-institution-manager.test.ts src/app/actions/detach-institution-member.test.ts src/app/actions/search-students-for-institution.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - All 5 files exist under `src/app/actions/`
    - Each file contains the `vi.mock("next/navigation"...)` block (copied from grant-enrollment.test.ts)
    - Each file contains at least one `describe(...)` block named after the production function
    - Each file contains at least 3 `it.todo(...)` placeholders (validates names are pre-written)
    - Each file contains the `scaffold sentinel` test that calls `expect(true).toBe(true)` (one passing assertion per file)
    - `npx vitest run <file>` exits 0 for each (the sentinel passes; todos are pending)
    - `npm run test:ci` exits 0 (no regression on the existing 38 tests)
  </acceptance_criteria>
  <done>
Five action-test scaffolds exist with verbatim mock boilerplate, named todos covering INST-08 acceptance behaviors, and one passing sentinel each. Vitest run on each file passes; full suite runs green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Create queries.test.ts + schema.test.ts test scaffolds in src/lib/institutions/</name>
  <files>src/lib/institutions/queries.test.ts, src/lib/institutions/schema.test.ts</files>
  <read_first>
    - src/lib/courses/queries.test.ts (full file — analog for queries.test.ts pattern with mocked Supabase chain)
    - src/lib/courses/schema.test.ts if it exists, OR src/app/actions/grant-enrollment.test.ts for Zod test pattern (look at how validation failures are asserted)
    - .planning/phases/05-b2b-institution-manager/05-PATTERNS.md §src/lib/institutions/queries.ts (analog mapping) and §src/lib/institutions/schema.ts (analog mapping)
    - .planning/phases/05-b2b-institution-manager/05-RESEARCH.md §Validation Architecture §Phase Requirements → Test Map (rows for INST-06 and INST-07)
  </read_first>
  <action>
Create two test files in `src/lib/institutions/`. Same scaffolding pattern as Task 1 (vi.mock block + it.todo + sentinel). The directory `src/lib/institutions/` does NOT exist yet — create it.

**File 1 — `src/lib/institutions/queries.test.ts`** (covers INST-06 + INST-07):

```typescript
// src/lib/institutions/queries.test.ts
//
// Wave 0 scaffold for INST-06 + INST-07: per-team progress matrix + certs queries.
// Production module created in plan 05-03; production data populated by 05-05 actions.
//
// Pattern source: src/lib/courses/queries.test.ts (mocked Supabase chain pattern).

import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("@/lib/supabase/server", () => ({ createSupabaseServerClient: vi.fn() }));
vi.mock("@/lib/supabase/admin", () => ({ createSupabaseAdminClient: vi.fn() }));
vi.mock("@/lib/logger", () => ({
  logger: { debug: vi.fn(), info: vi.fn(), warn: vi.fn(), error: vi.fn() },
}));

// Plan 05-03 will add:
// import { getInstitutionForManager, getInstitutionMembersWithProgress, getInstitutionCertificates } from "./queries";

describe("getInstitutionForManager", () => {
  beforeEach(() => vi.clearAllMocks());

  it.todo("returns null for orphan manager (zero institution_members rows for user_id with role='manager')");
  it.todo("returns the resolved institution row when user is the manager of exactly one institution");
  it.todo("uses the RLS-respecting server client (NOT admin client) — verifies CONTEXT D-04");
});

describe("getInstitutionMembersWithProgress", () => {
  beforeEach(() => vi.clearAllMocks());

  it.todo("returns [] when no members are linked to the institution");
  it.todo("only returns members of the requested institutionId (verifies INST-06 isolation)");
  it.todo("includes expired enrollments with enrollmentExpired: true (verifies ENR-04 + D-12 admin bypass)");
  it.todo("computes completionPercentage = round(completedLessons / totalLessons * 100)");
  it.todo("excludes deleted lessons (deleted_at IS NOT NULL) from totalLessons");
  it.todo("uses admin client — verifies D-12 bypass (justified by RLS expired-enrollment carve-out)");
});

describe("getInstitutionCertificates", () => {
  beforeEach(() => vi.clearAllMocks());

  it.todo("returns certs ordered by issued_at DESC (verifies UI-SPEC sort)");
  it.todo("only returns certs for users in the institution");
  it.todo("includes student_name, course_title, issued_at, certificate_code in each row");
  it.todo("returns [] on Supabase error (logs via logger.error, does not throw)");
});

it("scaffold sentinel — replace when production module exists", () => {
  expect(true).toBe(true);
});
```

**File 2 — `src/lib/institutions/schema.test.ts`** (covers Zod schemas):

```typescript
// src/lib/institutions/schema.test.ts
//
// Wave 0 scaffold for INST-08 input validation.
// Production schemas created in plan 05-03.

import { beforeEach, describe, expect, it, vi } from "vitest";

// Plan 05-03 will add:
// import {
//   createInstitutionSchema, updateInstitutionSchema,
//   attachMemberSchema, inviteMemberSchema,
//   promoteManagerSchema, demoteManagerSchema, detachMemberSchema,
// } from "./schema";

describe("createInstitutionSchema", () => {
  beforeEach(() => vi.clearAllMocks());

  it.todo("rejects empty name with 'Nome é obrigatório.'");
  it.todo("rejects slug with uppercase letters (regex /^[a-z0-9]+(?:-[a-z0-9]+)*$/)");
  it.todo("rejects slug with spaces");
  it.todo("rejects invalid email in contact_email");
  it.todo("treats contact_email '' as undefined (normalizeOptionalText preprocessor)");
  it.todo("accepts minimal valid payload { name, slug }");
  it.todo("trims leading/trailing whitespace from name");
});

describe("updateInstitutionSchema", () => {
  it.todo("requires institutionId UUID in addition to base fields");
  it.todo("rejects non-UUID institutionId");
});

describe("attachMemberSchema", () => {
  it.todo("requires institution_id (uuid) and profile_id (uuid)");
  it.todo("rejects non-uuid institution_id");
});

describe("inviteMemberSchema", () => {
  it.todo("requires institution_id (uuid), full_name (min 1), email (valid email)");
  it.todo("trims and lowercases email");
});

describe("promoteManagerSchema", () => {
  it.todo("requires institution_id, profile_id, institution_slug");
});

describe("detachMemberSchema", () => {
  it.todo("requires institution_id, profile_id, institution_slug");
});

it("scaffold sentinel — replace when production module exists", () => {
  expect(true).toBe(true);
});
```

Both files: include the sentinel passing test at the bottom so the file has at least one executable assertion. Do NOT import the production module in Wave 0 — plan 05-03 will uncomment those imports.
  </action>
  <verify>
    <automated>npx vitest run src/lib/institutions/queries.test.ts src/lib/institutions/schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - Directory `src/lib/institutions/` exists
    - File `src/lib/institutions/queries.test.ts` exists; contains 3 `describe` blocks (`getInstitutionForManager`, `getInstitutionMembersWithProgress`, `getInstitutionCertificates`); contains at least one `it.todo` referencing "verifies INST-06 isolation"; sentinel test passes
    - File `src/lib/institutions/schema.test.ts` exists; contains describe blocks for at least 5 schemas; sentinel test passes
    - `npx vitest run src/lib/institutions/queries.test.ts` exits 0
    - `npx vitest run src/lib/institutions/schema.test.ts` exits 0
    - `npm run test:ci` exits 0 (no regression)
  </acceptance_criteria>
  <done>
Both lib-test scaffolds exist with named todos covering INST-06/INST-07 (queries) and INST-08 (schema). Vitest runs green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Create middleware.test.ts test scaffold (INST-05)</name>
  <files>middleware.test.ts</files>
  <read_first>
    - middleware.ts (entire file — confirms current 3-ring shape and exports)
    - vitest.config.ts (verify root-level test files are included by vitest's `include` glob; if not, place file under src/ instead — see action below)
    - src/app/actions/grant-enrollment.test.ts (mock pattern for Supabase server client + fetchUserRole)
    - .planning/phases/05-b2b-institution-manager/05-VALIDATION.md §Wave 0 Requirements (middleware test todos)
  </read_first>
  <action>
First read `vitest.config.ts` to confirm whether root-level `*.test.ts` files are picked up. CLAUDE.md says vitest includes `src/**/*.test.{ts,tsx}` — this would EXCLUDE `middleware.test.ts` at repo root.

**Resolution:** If vitest.config.ts `test.include` is `["src/**/*.test.{ts,tsx}"]` only, place the test at `src/middleware.test.ts` and import from `../middleware` — Next.js still resolves the root `middleware.ts` for routing (this test file in src/ does NOT change Next.js routing, it only colocates a test).

**Decision rule:** if vitest.config.ts has only `src/**/*` includes, create `src/middleware.test.ts` (NOT root-level `middleware.test.ts`). If the config explicitly includes root-level test files, create `middleware.test.ts` at repo root. Document the choice in the file header comment.

**File contents (assumes the src/ location; adjust import path if root):**

```typescript
// src/middleware.test.ts (placed under src/ to match vitest include glob)
//
// Wave 0 scaffold for INST-05: /gestor route gating.
// Production middleware extension lands in plan 05-06.
//
// Pattern source: grant-enrollment.test.ts mock boilerplate.
// NOTE: Next.js middleware testing in this repo has no precedent. We test the
// pure routing logic by mocking @supabase/ssr and @/lib/auth/roles, then
// invoking the exported `middleware(request)` with synthesized NextRequest objects.

import { beforeEach, describe, expect, it, vi } from "vitest";

vi.mock("@supabase/ssr", () => ({
  createServerClient: vi.fn(() => ({
    auth: {
      getUser: vi.fn().mockResolvedValue({ data: { user: null }, error: null }),
    },
  })),
}));

vi.mock("@/lib/auth/roles", () => ({ fetchUserRole: vi.fn() }));

// Plan 05-06 will add:
// import { middleware } from "../middleware";  // <-- adjust path if file is at repo root
// import { NextRequest } from "next/server";

describe("middleware /gestor gate (INST-05)", () => {
  beforeEach(() => vi.clearAllMocks());

  it.todo("redirects unauthenticated request on /gestor to /login?redirectTo=/gestor");
  it.todo("redirects authenticated user with role='student' on /gestor to /dashboard");
  it.todo("redirects authenticated user with role='admin' on /gestor to /admin/instituicoes (D-02)");
  it.todo("allows authenticated user with role='institution_manager' on /gestor to proceed");
  it.todo("does NOT query institution_members in middleware — orphan check is in /gestor/page.tsx (D-04 + Pitfall 1)");
});

describe("middleware /admin gate (regression — must still work)", () => {
  beforeEach(() => vi.clearAllMocks());

  it.todo("redirects authenticated user with role='institution_manager' on /admin to /dashboard (admin-only ring still enforced)");
  it.todo("allows authenticated user with role='admin' on /admin to proceed");
});

describe("middleware matcher config", () => {
  it.todo("matcher includes '/gestor/:path*' (exact verification by importing config and asserting array contents)");
});

it("scaffold sentinel — replace when production extension exists", () => {
  expect(true).toBe(true);
});
```

**Critical:** The `import { middleware } from "../middleware"` line MUST stay commented out in Wave 0. The current middleware.ts does NOT have the `/gestor` ring, so the it.todo asserts cannot run. Plan 05-06 will uncomment this import and replace todos with actual `it(...)` test bodies.
  </action>
  <verify>
    <automated>cat vitest.config.ts | grep -E "include|test:" | head -10 &amp;&amp; (test -f src/middleware.test.ts || test -f middleware.test.ts) &amp;&amp; npx vitest run $(test -f src/middleware.test.ts &amp;&amp; echo src/middleware.test.ts || echo middleware.test.ts)</automated>
  </verify>
  <acceptance_criteria>
    - File exists at either `src/middleware.test.ts` (if vitest.config include is src-only) or `middleware.test.ts` (if root included)
    - File contains `describe("middleware /gestor gate (INST-05)"`
    - File contains at least 4 `it.todo` placeholders covering: unauth, student, admin, institution_manager paths
    - File contains the regression-guard describe for `/admin` gate
    - File header comment documents the file-location decision (root vs src/) and references vitest.config.ts
    - `npx vitest run <the-file>` exits 0
    - `npm run test:ci` exits 0
  </acceptance_criteria>
  <done>
Middleware test scaffold exists at the correct location (src/ or root, per vitest.config). Five gestor-gate todos + admin regression guard + matcher config todo. Sentinel passes. Plan 05-06 can uncomment the production import and fill in test bodies.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

This plan introduces ZERO production code or runtime surface. All files are test-only and run inside Node + Vitest. No new trust boundaries.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-02-01 | Tampering | Test file imports a real Supabase client (would hit live DB during CI) | mitigate | Every scaffold's vi.mock block intercepts `@/lib/supabase/server`, `@/lib/supabase/admin`, `@supabase/ssr` BEFORE any test code runs. Verified by Task 1/2/3 acceptance: vi.mock blocks present in every file. |
| T-05-02-02 | Tampering | Test scaffold accidentally imports production module that doesn't exist yet, breaking CI | mitigate | All production-module imports are commented out (`// Plan 05-XX will add: ...`). Sentinel test (`expect(true).toBe(true)`) provides at least one assertion per file. CI run in acceptance proves no breakage. |
| T-05-02-03 | Information Disclosure | Test file leaks real production credentials via mock | accept | Mocks return synthetic values; no `getEnv()` or `process.env` access in test bodies. Out of scope for test-only plan. |
</threat_model>

<verification>
- [ ] All 8 test files exist (5 actions + 2 lib + 1 middleware)
- [ ] Each file imports `vi`, `describe`, `it`, `expect`, `beforeEach` from "vitest"
- [ ] Each file has the standardized vi.mock block (or middleware-specific equivalent)
- [ ] Each file has the `scaffold sentinel` test that passes
- [ ] `npm run test:ci` exits 0 (sentinels pass + 38 existing tests still pass = 46+ tests reported)
- [ ] No production module is imported in Wave 0 (all such imports commented out with "Plan 05-XX will add" comment)
- [ ] Plans 05-03 through 05-06 have a paired test file ready to fill — no `MISSING — Wave 0 must create…` anywhere in their `<verify>` blocks
</verification>

<success_criteria>
- 8 test scaffolds created at the paths listed in `files_modified`
- Every Wave 1+ implementation task can verify itself with `npx vitest run <test-file>` instead of declaring `MISSING`
- Test names (in `it.todo` strings) read as a TDD specification: each one names a behavior the implementation must satisfy
- `npm run test:ci` runs green; `npm run lint` runs green
- The 38 existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/05-b2b-institution-manager/05-02-SUMMARY.md` capturing:
- Total test count before/after (e.g., "38 → 46: 8 sentinels added")
- File location decision for middleware.test.ts (src/ vs root, with vitest.config.ts evidence)
- List of `it.todo` counts per file (proves the names are pre-written)
- Note: this plan UNBLOCKS the `<automated>` verifies in plans 05-03 through 05-06
</output>
