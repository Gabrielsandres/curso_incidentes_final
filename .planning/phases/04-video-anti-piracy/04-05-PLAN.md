---
phase: "04-video-anti-piracy"
plan: "05"
type: execute
wave: 3
depends_on: ["04-03", "04-04"]
files_modified:
  - docs/anti-piracy.md
autonomous: false
requirements:
  - AP-04

user_setup:
  - service: bunny-stream
    why: "Production video serving requires Bunny Stream credentials"
    env_vars:
      - name: BUNNY_STREAM_TOKEN_KEY
        source: "Bunny Dashboard → Stream library → API → Token Authentication"
      - name: BUNNY_STREAM_LIBRARY_ID
        source: "Bunny Dashboard → Stream library → Library ID (numeric)"
      - name: BUNNY_STREAM_TOKEN_TTL_SECONDS
        source: "Set to 3600 (default). Increase up to 14400 if needed. Must not exceed 14400."
    dashboard_config:
      - task: "Enable Token Authentication on the Bunny Stream library"
        location: "Bunny Dashboard → Stream → [Library Name] → Security → Token Authentication → Enable"

must_haves:
  truths:
    - "docs/anti-piracy.md exists and honestly documents that overlay is deterrence, not DRM"
    - "docs/anti-piracy.md states screen recording remains possible"
    - "docs/anti-piracy.md describes what the system does: CSS overlay with student email, rotating position, 10-15% opacity"
    - "docs/anti-piracy.md describes what the system does NOT do: no IP-binding, no DRM, no server-side recording detection"
    - "The document covers: what is protected, what is not protected, honest ceiling statement"
  artifacts:
    - path: "docs/anti-piracy.md"
      provides: "Honest anti-piracy documentation per AP-04"
      contains: "deterrence"
  key_links: []

---

<objective>
Create `docs/anti-piracy.md` documenting the realistic protection ceiling of the Phase 4 anti-piracy measures, and run a final human checkpoint to verify the full player flow end-to-end.

AP-04 requires honest documentation stating that the overlay is deterrence, not DRM, and that screen recording remains possible. The docs/ directory already exists from Phase 1.

Purpose: Fulfills AP-04 (documentation). Provides honest communication to MDHE about what protection they have and what they do not. The checkpoint verifies the full video player flow works before the phase is marked complete.

Output: 1 new file (docs/anti-piracy.md) + human verification checkpoint.
</objective>

<execution_context>
@/c/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.planning/phases/04-video-anti-piracy/04-RESEARCH.md
</execution_context>

<context>
@/c/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.planning/ROADMAP.md
@/c/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.planning/phases/04-video-anti-piracy/04-CONTEXT.md
@/c/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.planning/phases/04-video-anti-piracy/04-03-SUMMARY.md
@/c/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.planning/phases/04-video-anti-piracy/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/anti-piracy.md with honest ceiling documentation</name>
  <files>docs/anti-piracy.md</files>
  <read_first>
    - docs/ directory listing (confirm docs/ exists; it does — Phase 1 created DEPLOY-CHECKLIST.md there)
    - .planning/phases/04-video-anti-piracy/04-RESEARCH.md (Security Domain section + Common Pitfalls — use as source of truth)
    - .planning/phases/04-video-anti-piracy/04-CONTEXT.md (D-04 through D-08 for accurate feature description)
  </read_first>
  <action>
    Create docs/anti-piracy.md with the following structure (content below is required; Claude must write it in full):

    ```markdown
    # Anti-Piracy Measures — Plataforma MDHE

    **Data:** [today's date]
    **Escopo:** Proteção de conteúdo de vídeo na plataforma de cursos MDHE

    ---

    ## O que está implementado

    ### Overlay de marca d'água (CSS watermark)

    - O player de aula exibe o **e-mail completo do aluno** sobre o vídeo em baixa opacidade (aproximadamente 12%)
    - A posição do overlay muda entre os **4 cantos do player a cada 30 segundos**
    - O overlay é implementado como `div` CSS absolutamente posicionado sobre o `<iframe>` do player
    - `pointer-events: none` — o overlay não interfere com os controles do player
    - `aria-hidden="true"` — invisível para leitores de tela (não é conteúdo funcional)
    - `user-select: none` — impede seleção de texto trivial

    ### URLs assinadas do Bunny Stream (produção)

    - Em produção, vídeos Bunny Stream são servidos via **URLs assinadas com TTL de 1 hora (3600 segundos)**
    - O token é gerado server-side (RSC Next.js) usando `SHA256(tokenKey + videoId + expiresUnix)`
    - A chave de assinatura (`BUNNY_STREAM_TOKEN_KEY`) nunca é enviada ao browser
    - Cada carregamento de página de aula gera um novo token — re-mint a cada acesso

    ### Proteção de credenciais server-side

    - Token signing acontece exclusivamente no RSC (`page.tsx`) — nunca em Client Components
    - A chave Bunny está no `serverSchema` do Zod (`src/lib/env.ts`) e falha no boot em produção se ausente
    - YouTube está disponível **apenas em desenvolvimento** (`NODE_ENV !== 'production'`); tentativa de usar em produção resulta em erro 500

    ---

    ## O que NÃO está implementado (teto realista da proteção)

    ### Gravação de tela continua possível

    **Esta é a limitação mais importante:** qualquer pessoa com acesso ao vídeo pode gravá-lo usando:
    - Ferramentas de gravação de tela (OBS Studio, QuickTime, Xbox Game Bar, etc.)
    - Extensões de browser
    - Dispositivo externo apontado para a tela

    O overlay de e-mail aparecerá na gravação — isso é o objetivo (identifica o vazador). Mas não impede a captura do conteúdo.

    ### Sem DRM (Widevine / PlayReady / FairPlay)

    DRM completo (Widevine, PlayReady, FairPlay) foi explicitamente descartado do v1:
    - Custo alto de implementação e operação
    - Requer suporte do provedor de vídeo + browser + sistema operacional
    - Bypassável via gravação de tela em qualquer dispositivo externo
    - Não se justifica para o perfil de risco B2B da MDHE

    ### Sem IP-binding nos tokens

    Os tokens Bunny **não incluem o IP do cliente**. Isso é intencional:
    - O Brasil tem CGNAT extenso (Claro, Vivo, TIM) — múltiplos usuários compartilham o mesmo IP público
    - IP-binding causaria falhas de reprodução legítimas em redes móveis brasileiras
    - A decisão está documentada em REQUIREMENTS.md (AP-03)

    ### Sem detecção de gravação de tela

    Não há JavaScript que detecte tentativas de gravação ou screenshots. Implementações deste tipo são facilmente bypassáveis e criam falsos positivos.

    ---

    ## Propósito declarado: deterrence, não DRM

    As medidas implementadas funcionam como **dissuasão (deterrence)**:

    1. **Identificação:** O e-mail do aluno na gravação identifica a fonte do vazamento
    2. **Custo psicológico:** A presença do e-mail visível aumenta o custo percebido de vazar conteúdo
    3. **URLs de curta duração:** Tokens de 1 hora reduzem a janela de compartilhamento de links diretos do Bunny

    Isso é suficiente para o perfil de risco B2B corporativo da MDHE, onde alunos são profissionais com vínculo institucional.

    ---

    ## Recomendações operacionais

    - **Rotacionar `BUNNY_STREAM_TOKEN_KEY`** periodicamente (trimestral) via painel Bunny
    - **Monitorar compartilhamento de acesso** — um login com múltiplos IPs simultâneos indica conta compartilhada
    - **Comunicar honestamente aos clientes B2B** que o sistema usa marca d'água de identificação, não DRM
    - **Se um vazamento ocorrer:** o e-mail do aluno na gravação permite identificar a origem e tomar ação disciplinar

    ---

    *Documento criado durante a Phase 4: Video & Anti-Piracy*
    *Revisão: necessária se o modelo de ameaça mudar (ex.: inclusão de DRM no roadmap)*
    ```

    Write the file. Use today's date in the Data field (2026-04-30).
  </action>
  <verify>
    <automated>cd /c/Users/gabri/OneDrive/Área\ de\ Trabalho/Pessoal/curso_incidentes_final && test -f docs/anti-piracy.md && grep -c "deterrence\|deterrência\|dissuasão" docs/anti-piracy.md</automated>
  </verify>
  <acceptance_criteria>
    - docs/anti-piracy.md file exists
    - grep "screen recording\|gravação de tela" docs/anti-piracy.md returns a match (honest limitation documented)
    - grep "deterrence\|dissuasão\|deterrência" docs/anti-piracy.md returns a match
    - grep "DRM" docs/anti-piracy.md returns a match (DRM explicitly mentioned and its absence explained)
    - grep "IP-binding\|CGNAT" docs/anti-piracy.md returns a match (AP-03 rationale documented)
    - grep "SHA256\|token" docs/anti-piracy.md returns a match (Bunny signing described)
    - File length > 50 lines (not a stub)
  </acceptance_criteria>
  <done>docs/anti-piracy.md exists with honest ceiling documentation; all acceptance criteria pass</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification — full Phase 4 player flow</name>
  <what-built>
    Plans 01-04 built the complete video provider abstraction:
    - src/lib/video/ module (VideoProvider interface, Bunny adapter with SHA256 signing, YouTube adapter with prod guard)
    - Env vars for Bunny credentials validated at boot in production
    - getLessonWithCourseContext now fetches video_provider + video_external_id
    - RSC page.tsx calls getPlayableSource server-side and passes pre-resolved props
    - LessonPlayer refactored: no YouTube IFrame API, postMessage-based auto-completion, WatermarkOverlay for Bunny
    - createLessonSchema + createLessonAction updated to write video_provider/video_external_id
    - add-lesson-form.tsx: video provider selector with isProduction guard
    - docs/anti-piracy.md: honest ceiling documentation
  </what-built>
  <how-to-verify>
    Run the dev server: npm run dev

    1. **Admin — Create a new lesson with YouTube:**
       - Go to http://localhost:3000/admin/cursos/[any-course-slug]/modulos/[module-id]
       - Click "Adicionar aula"
       - Verify the form shows a "VÍDEO" section with provider selector ("Bunny Stream" default + "YouTube (apenas dev)")
       - Select "YouTube (apenas dev)", enter a YouTube video ID (e.g., dQw4w9WgXcQ)
       - Submit the form
       - Verify lesson was created (redirects or shows success)

    2. **Student — Lesson page with YouTube in dev:**
       - Navigate to the lesson page for the newly created lesson
       - Verify: iframe renders (no YouTube IFrame API script injection in page source)
       - Verify: NO watermark overlay (YouTube dev mode, D-04)
       - Verify: "Marcar aula como concluída" button is visible
       - Verify: browser DevTools console shows NO errors related to YouTube API or player

    3. **Auto-completion (YouTube postMessage):**
       - Watch the YouTube video to the end (or open DevTools Console and run:
         `window.postMessage(JSON.stringify({event: 'infoDelivery', info: {playerState: 0}}), '*')`)
       - Verify: lesson marked as completed (button state changes to "Aula concluída")
       - Verify: if it's the last lesson, completion banner appears

    4. **Typecheck + lint + tests (final CI gate):**
       Run: npm run typecheck && npm run lint && npm run test:ci
       All must exit 0.

    5. **Production guard (optional local verification):**
       Temporarily set NODE_ENV=production in .env.local and navigate to a lesson with video_provider='youtube'
       Verify: page returns 500 error (expected behavior per VID-02)
       Restore NODE_ENV after test.

    Note: Bunny Stream production flow cannot be verified locally without credentials. The unit tests in Plan 01 prove the signing algorithm is correct. Full Bunny verification happens after production deployment.
  </how-to-verify>
  <resume-signal>Type "approved" if all dev-mode checks pass, or describe any issues found</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/ → operators | Documentation is internal; no user-facing threat |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Information Disclosure | docs/anti-piracy.md content | accept | Internal ops document; contains no secrets; honest limitation disclosure is intentional |
</threat_model>

<verification>
```bash
# File existence and content checks:
test -f docs/anti-piracy.md && echo "EXISTS"
grep -c "gravação de tela\|screen recording" docs/anti-piracy.md
grep -c "dissuasão\|deterrence" docs/anti-piracy.md

# Final CI gate (run after human checkpoint passes):
npm run typecheck && npm run lint && npm run test:ci
```
</verification>

<success_criteria>
- docs/anti-piracy.md exists with honest ceiling documentation (AP-04 fulfilled)
- Document explicitly states: screen recording is still possible, overlay is deterrence not DRM, no IP-binding and why
- Human checkpoint: admin create-form shows video section; lesson page renders iframe; no YouTube IFrame API script; auto-completion via postMessage works
- npm run typecheck, lint, test:ci all pass (final CI gate)
- All 9 phase requirement IDs covered:
  - VID-01: VideoProvider interface in src/lib/video/ — Plan 01
  - VID-02: YouTube prod guard — Plan 01
  - VID-03: Bunny SHA256 signing + TTL env var — Plans 01 + 02
  - VID-04: Server-side token signing — Plans 01 + 03
  - VID-05: video_provider/video_external_id columns + admin forms — Plans 02 + 04
  - AP-01: CSS overlay with email, pointer-events:none — Plan 03
  - AP-02: Bunny TTL ≤ 4h, re-minted on page load — Plans 01 + 03
  - AP-03: No IP-binding in tokens — Plan 01 (SHA256 formula has no IP)
  - AP-04: docs/anti-piracy.md — this plan
</success_criteria>

<output>
After completion, create `.planning/phases/04-video-anti-piracy/04-05-SUMMARY.md` documenting:
- Confirmation that docs/anti-piracy.md was created
- Human checkpoint outcome (what was verified, any issues found)
- Final CI gate results (typecheck/lint/test:ci)
- Phase 4 completion declaration
</output>
