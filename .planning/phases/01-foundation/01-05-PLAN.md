---
phase: 01-foundation
plan: 05
type: execute
wave: 3
depends_on:
  - 01-01-PLAN
  - 01-02-PLAN
  - 01-03-PLAN
files_modified:
  - docs/DEPLOY-CHECKLIST.md
autonomous: false
requirements:
  - OPS-04
  - OPS-05
  - MKT-03

must_haves:
  truths:
    - "docs/DEPLOY-CHECKLIST.md exists with pt-BR section headings"
    - "Checklist contains required env vars section with SUPABASE_SERVICE_ROLE_KEY and other server vars"
    - "Checklist documents migration apply order: 0012 before 0013"
    - "Checklist has post-deploy smoke tests including /health and login flow"
    - "Checklist has Resend SMTP section (filled in after Plan 04)"
    - "CI (lint + test:ci + build) passes green on the phase 1 branch"
    - "/health route still returns {status, uptime, timestamp, version} shape"
  artifacts:
    - path: "docs/DEPLOY-CHECKLIST.md"
      provides: "Full deploy checklist with env vars, migration order, smoke tests, email section, rollback guidance"
      contains: "SUPABASE_SERVICE_ROLE_KEY"
    - path: "docs/DEPLOY-CHECKLIST.md"
      contains: "0012"
    - path: "docs/DEPLOY-CHECKLIST.md"
      contains: "/health"
  key_links:
    - from: "docs/DEPLOY-CHECKLIST.md"
      to: "src/lib/env.ts"
      via: "env var names cross-referenced to schema location"
      pattern: "serverSchema"
    - from: "docs/DEPLOY-CHECKLIST.md"
      to: "supabase/migrations/0012"
      via: "migration apply order documented"
      pattern: "0012.*0013"
---

<objective>
Create the deploy checklist document and verify CI green after all Phase 1 code changes.

Purpose: OPS-05 requires a documented deploy runbook. OPS-04 requires CI to pass. MKT-03 requires the /health route to remain operational. This plan ties together the Phase 1 deliverables — the checklist is the operator's reference for deploying safely, and CI green is the engineering gate.

Output:
- docs/DEPLOY-CHECKLIST.md: complete operational checklist (pt-BR headings)
- CI verification: lint + test:ci + build all green on the phase 1 branch
- /health smoke: route preserved and returning correct shape
</objective>

<execution_context>
@/c/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.claude/get-shit-done/workflows/execute-plan.md
@/c/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create docs/DEPLOY-CHECKLIST.md (D-16, OPS-05)</name>
  <files>docs/DEPLOY-CHECKLIST.md</files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Q9: full DEPLOY-CHECKLIST.md skeleton — use verbatim, translate headings to pt-BR)
    - src/lib/env.ts (confirm current env var names in serverSchema and clientSchema before writing the table)
    - src/app/health/route.ts (confirm the JSON shape returned — must match the smoke test specification)
  </read_first>
  <action>
Create `docs/DEPLOY-CHECKLIST.md` using the skeleton from RESEARCH.md Q9 (lines 730–819 of 01-RESEARCH.md) as the base. The skeleton is already well-structured — use it verbatim with these adjustments:

1. All section headings must be in **pt-BR** (e.g., "## 1. Variáveis de Ambiente" not "## 1. Environment Variables")
2. The skeleton is already in pt-BR — use it directly
3. In section 2 (Migrações Pendentes), ensure the warning block explicitly says:
   "Abra dois queries separados no SQL Editor — NÃO aplique 0012 e 0013 no mesmo bloco"
4. In section 3.1 (Health Check), replace the placeholder URL with the actual production URL pattern
5. Add a section 5 for Resend email configuration with placeholder fields for the operator to fill in:

```markdown
## 5. Configuração de Email (Resend SMTP)

Status: `[ ]` Domínio verificado no Resend (SPF + DKIM verdes)

| Campo | Valor configurado |
|-------|------------------|
| EMAIL_FROM | `_____________________________` |
| Conta Resend | `_____________________________` |
| API Key armazenada em | `_____________________________` |
| Provedor DNS onde os registros foram adicionados | `_____________________________` |

**Testes de entregabilidade realizados:**
- `[ ]` Gmail: email de recuperação chegou à caixa de entrada (não spam) em até 60s
- `[ ]` Outlook/Hotmail: email de recuperação chegou à caixa de entrada (não spam) em até 60s

**Configuração no painel do Supabase Auth:**
- Host: `smtp.resend.com`
- Porta: `465`
- Usuário: `resend`
- Senha: API key do Resend (permissão "Sending access")
```

The complete file structure follows RESEARCH.md Q9 exactly. Do NOT include actual secret values — only variable names and placeholder lines for the operator to fill.

**Critical check:** The file must contain:
- `SUPABASE_SERVICE_ROLE_KEY` (with a note that it is prod-required after OPS-01)
- `0012` and `0013` migration entries in the correct apply order
- `/health` smoke test with expected JSON shape
- Rollback section at the end
  </action>
  <verify>
    <automated>test -f docs/DEPLOY-CHECKLIST.md && grep -c "SUPABASE_SERVICE_ROLE_KEY" docs/DEPLOY-CHECKLIST.md</automated>
  </verify>
  <acceptance_criteria>
    - `test -f docs/DEPLOY-CHECKLIST.md` exits 0
    - `grep "SUPABASE_SERVICE_ROLE_KEY" docs/DEPLOY-CHECKLIST.md` matches
    - `grep "0012" docs/DEPLOY-CHECKLIST.md` matches
    - `grep "0013" docs/DEPLOY-CHECKLIST.md` matches
    - `grep "smtp.resend.com" docs/DEPLOY-CHECKLIST.md` matches
    - `grep "/health" docs/DEPLOY-CHECKLIST.md` matches
    - `grep "serverSchema" docs/DEPLOY-CHECKLIST.md` matches (env vars cross-referenced)
    - `grep "Variáveis de Ambiente" docs/DEPLOY-CHECKLIST.md` matches (pt-BR heading)
    - `grep "Migrações" docs/DEPLOY-CHECKLIST.md` matches (pt-BR heading)
    - File does NOT contain any actual secret values (API keys, passwords) — only placeholder lines
  </acceptance_criteria>
  <done>docs/DEPLOY-CHECKLIST.md created with pt-BR headings, env var table, migration order, smoke tests, email section, rollback guidance</done>
</task>

<task type="checkpoint:human-verify">
  <name>Verificação de CI + smoke test do /health (OPS-04, MKT-03)</name>
  <what-built>
All Phase 1 code changes (Plans 01–03) are committed. docs/DEPLOY-CHECKLIST.md is created. The CI pipeline must be green, and the /health route must return the expected JSON shape.
  </what-built>
  <how-to-verify>
**STEP 1 — Run full CI suite locally (OPS-04):**

```bash
npm run lint
npm run typecheck
npm run test:ci
npm run build
```

All four commands must exit 0. If any fails:
- `lint`: fix the ESLint warning/error in the indicated file
- `typecheck`: usually means database.types.ts is inconsistent — re-check the edit from Plan 03
- `test:ci`: identify the failing test from the verbose output and fix
- `build`: usually means a type error that typecheck missed in a tsx file

**STEP 2 — Verify /health route is unbroken (MKT-03):**

In development:
```bash
npm run dev
```

In a separate terminal:
```bash
curl -s http://localhost:3000/health | python3 -m json.tool
```

Expected output shape:
```json
{
  "status": "ok",
  "uptime": 1.234,
  "timestamp": "2026-04-27T...",
  "version": "..."
}
```

All four keys must be present. If any key is missing, do NOT modify the health route for this phase (D-19: no code change unless regression). Check if an env validation change in Plan 01 caused a cold-start crash.

**STEP 3 — Run the targeted test suite one last time:**

```bash
npx vitest run src/lib/env.test.ts src/lib/observability/sentry.test.ts src/lib/certificates/pdf.test.ts src/lib/auth/profiles.test.ts --reporter=verbose
```

All tests must pass.
  </how-to-verify>
  <resume-signal>Type "CI verde" if all four CLI commands and the /health check pass. Describe any failures if they occur.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| docs/DEPLOY-CHECKLIST.md → secrets | Checklist must reference variable NAMES only — never actual key values |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T1 | Information Disclosure | docs/DEPLOY-CHECKLIST.md | mitigate | File must contain only variable names (e.g., `SUPABASE_SERVICE_ROLE_KEY`) and placeholder lines — never actual secrets. The acceptance criteria explicitly check for this. The file is committed to git, so secrets in it would be leaked to anyone with repo access. |
</threat_model>

<verification>
Final phase gate before marking Phase 1 complete:

```bash
npm run lint && npm run typecheck && npm run test:ci && npm run build
```

Plus manual /health smoke test.
</verification>

<success_criteria>
1. `test -f docs/DEPLOY-CHECKLIST.md` exits 0
2. `grep "Variáveis de Ambiente" docs/DEPLOY-CHECKLIST.md` matches (pt-BR)
3. `grep "0012" docs/DEPLOY-CHECKLIST.md` matches
4. `npm run lint` exits 0 (zero warnings)
5. `npm run typecheck` exits 0
6. `npm run test:ci` exits 0 — all tests pass
7. `npm run build` exits 0
8. `curl -s http://localhost:3000/health` returns JSON with keys: status, uptime, timestamp, version
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-05-SUMMARY.md` following the summary template.
</output>
