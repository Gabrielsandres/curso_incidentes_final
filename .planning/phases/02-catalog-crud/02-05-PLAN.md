---
phase: 02-catalog-crud
plan: "05"
type: execute
wave: 3
depends_on: [02-02-PLAN, 02-03-PLAN, 02-04-PLAN]
files_modified:
  - src/app/admin/page.tsx
  - src/app/admin/course-manager.tsx
files_created:
  - src/components/admin/breadcrumb.tsx
  - src/components/admin/status-badge.tsx
  - src/components/admin/reorder-buttons.tsx
  - src/components/admin/confirmation-dialog.tsx
  - src/app/admin/cursos/page.tsx
  - src/app/admin/cursos/novo/page.tsx
  - src/app/admin/cursos/[slug]/page.tsx
  - src/app/admin/cursos/[slug]/modulos/[moduleId]/page.tsx
  - src/app/admin/cursos/[slug]/aulas/[lessonId]/page.tsx
autonomous: true
requirements: []
# All 7 CAT-* requirement IDs are covered by the action plans 02-02/02-03/02-04.
# This plan builds the RSC UI shell that surfaces those actions to the admin.
# Requirement coverage is informational: ui_for: [CAT-01, CAT-02, CAT-03, CAT-04, CAT-05, CAT-06, CAT-07]

must_haves:
  truths:
    - "Admin navigating to /admin is redirected to /admin/cursos"
    - "Admin sees the course list at /admin/cursos with status stats row"
    - "Admin can navigate to /admin/cursos/novo and get a blank course creation form"
    - "Admin can navigate to /admin/cursos/[slug] and edit a course with publish/archive actions"
    - "Admin can navigate to /admin/cursos/[slug]/modulos/[moduleId] and edit a module with lesson list"
    - "Admin can navigate to /admin/cursos/[slug]/aulas/[lessonId] and edit a lesson with material section"
    - "Breadcrumb component renders correct hierarchy on every nested page"
    - "StatusBadge component renders Rascunho/Publicado/Arquivado with correct Tailwind classes"
    - "ReorderButtons render up/down with disabled state at list boundaries"
    - "ConfirmationDialog traps focus and fires action only on Confirmar"
  artifacts:
    - path: "src/components/admin/breadcrumb.tsx"
      provides: "Reusable breadcrumb nav component"
      exports: ["Breadcrumb"]
    - path: "src/components/admin/status-badge.tsx"
      provides: "Course status badge + deriveCourseStatus helper"
      exports: ["StatusBadge", "deriveCourseStatus"]
    - path: "src/components/admin/reorder-buttons.tsx"
      provides: "Up/down reorder button pair for module and lesson lists"
      exports: ["ReorderButtons"]
    - path: "src/components/admin/confirmation-dialog.tsx"
      provides: "Accessible confirmation dialog for destructive actions"
      exports: ["ConfirmationDialog"]
    - path: "src/app/admin/cursos/page.tsx"
      provides: "RSC course list page with stats row and empty state"
    - path: "src/app/admin/cursos/novo/page.tsx"
      provides: "RSC new course page (blank form)"
    - path: "src/app/admin/cursos/[slug]/page.tsx"
      provides: "RSC course edit page with module list"
    - path: "src/app/admin/cursos/[slug]/modulos/[moduleId]/page.tsx"
      provides: "RSC module edit page with lesson list"
    - path: "src/app/admin/cursos/[slug]/aulas/[lessonId]/page.tsx"
      provides: "RSC lesson edit page with material section"
  key_links:
    - from: "src/app/admin/page.tsx"
      to: "/admin/cursos"
      via: "redirect() from next/navigation"
      pattern: "redirect.*admin/cursos"
    - from: "src/app/admin/cursos/page.tsx"
      to: "src/components/admin/status-badge.tsx"
      via: "import deriveCourseStatus and StatusBadge"
      pattern: "deriveCourseStatus"
    - from: "src/app/admin/cursos/[slug]/page.tsx"
      to: "src/components/admin/breadcrumb.tsx"
      via: "import Breadcrumb"
      pattern: "Breadcrumb"
---

<objective>
Build the RSC admin page tree for catalog management and the four shared UI components.

Purpose: Expose the server actions delivered by plans 02-02/03/04 to the admin through a clean page hierarchy. The pages are RSC-first; client state is minimal (dialog open/close, form pending via useFormStatus).

Output:
- 4 reusable components: Breadcrumb, StatusBadge, ReorderButtons, ConfirmationDialog
- 5 RSC admin pages: /admin/cursos, /admin/cursos/novo, /admin/cursos/[slug], /admin/cursos/[slug]/modulos/[moduleId], /admin/cursos/[slug]/aulas/[lessonId]
- src/app/admin/page.tsx replaced with redirect to /admin/cursos
- src/app/admin/course-manager.tsx simplified (CourseManager form extracted to reusable pieces)
</objective>

<execution_context>
@C:/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/gabri/OneDrive/Área de Trabalho/Pessoal/curso_incidentes_final/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-catalog-crud/02-PATTERNS.md
@.planning/phases/02-catalog-crud/02-UI-SPEC.md
@.planning/phases/02-catalog-crud/02-CONTEXT.md
@.planning/phases/02-catalog-crud/02-02-PLAN.md
@.planning/phases/02-catalog-crud/02-03-PLAN.md
@.planning/phases/02-catalog-crud/02-04-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Four reusable admin UI components</name>
  <files>
    src/components/admin/breadcrumb.tsx,
    src/components/admin/status-badge.tsx,
    src/components/admin/reorder-buttons.tsx,
    src/components/admin/confirmation-dialog.tsx
  </files>

  <read_first>
    1. .planning/phases/02-catalog-crud/02-PATTERNS.md — FIRST. Read full file. Section "Status/UI Components" (lines 858-942) has exact TSX for all four components.
    2. .planning/phases/02-catalog-crud/02-UI-SPEC.md — Component Library sections 7 (Badge), 8 (Breadcrumb), 9 (ReorderButtons), 11 (ConfirmationDialog) for exact Tailwind classes and ARIA.
    3. src/app/admin/page.tsx — current admin page for layout reference.
    4. src/app/admin/usuarios/user-manager.tsx — FeedbackBanner and FieldError patterns.
  </read_first>

  <action>
**A. `src/components/admin/breadcrumb.tsx`** — Server Component (no "use client").

```tsx
// Pure RSC — no state needed. Uses Link from next/link and ChevronRight from lucide-react.
// Props: items: Array<{ label: string; href?: string }>
// Rules from UI-SPEC Component 8:
//   - First item always "Catálogo" linking to /admin/cursos
//   - Last item: font-medium text-slate-900 + aria-current="page" + no href
//   - Separators: ChevronRight size={14} aria-hidden="true"
//   - nav aria-label="Navegação"
//   - ol className="flex items-center gap-1.5 text-sm text-slate-500"
```

Export named `Breadcrumb`. The component maps over `items`, renders `<Link>` for items with `href` that are not last, and `<span>` for last or hrefless items.

**B. `src/components/admin/status-badge.tsx`** — Server Component.

Export `StatusBadge({ status }: { status: "rascunho" | "publicado" | "arquivado" })` and `deriveCourseStatus(publishedAt: string | null, archivedAt: string | null)`.

Badge classes per UI-SPEC Component 7 (exact Tailwind):
- rascunho: `"inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold bg-slate-100 text-slate-600 border border-slate-300"`
- publicado: `"inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold bg-emerald-50 text-emerald-700 border border-emerald-200"`
- arquivado: `"inline-flex items-center rounded-full px-2.5 py-0.5 text-xs font-semibold bg-amber-50 text-amber-700 border border-amber-200"`

`deriveCourseStatus`: if `archivedAt` is not null → "arquivado"; else if `publishedAt` is not null → "publicado"; else → "rascunho".

Labels: Rascunho / Publicado / Arquivado.

**C. `src/components/admin/reorder-buttons.tsx`** — Client Component ("use client").

Props: `{ id: string; isFirst: boolean; isLast: boolean; upAction: (fd: FormData) => Promise<unknown>; downAction: (fd: FormData) => Promise<unknown> }`

Each button is a separate `<form action={upAction|downAction} method="post">` with a hidden `<input name="id" value={id} />` and a submit button using `useFormStatus()` for pending state.

Icon button classes from UI-SPEC Component 1 (icon variant):
`"inline-flex items-center justify-center rounded min-h-[44px] min-w-[44px] text-slate-500 transition hover:bg-slate-100 hover:text-slate-700 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-sky-500 disabled:opacity-70 disabled:cursor-not-allowed"`

aria-labels: "Mover para cima" / "Mover para baixo" (pt-BR per UI-SPEC Accessibility). Add `aria-disabled="true"` alongside `disabled` on boundary buttons. Icons: `ChevronUp` / `ChevronDown` from lucide-react, size={16}.

Wrap the two forms in `<div className="flex flex-col gap-0.5">`.

**D. `src/components/admin/confirmation-dialog.tsx`** — Client Component ("use client").

Props:
```typescript
interface ConfirmationDialogProps {
  open: boolean;
  onClose: () => void;
  title: string;
  body: string;
  confirmLabel: string;
  onConfirm: () => void;
  isPending?: boolean;
}
```

ARIA per UI-SPEC Component 11: `role="alertdialog"` (not "dialog"), `aria-modal="true"`, `aria-labelledby` pointing to h3 id.

Behavior:
- Overlay: `fixed inset-0 bg-slate-900/50 z-50 flex items-center justify-center p-4`. Click on overlay calls `onClose`.
- Panel: `relative bg-white rounded-2xl shadow-lg w-full max-w-md p-6 space-y-4`. `onClick={(e) => e.stopPropagation()}` to prevent overlay click.
- `useEffect` with `keydown` listener: if `e.key === "Escape"` and `open`, call `onClose()`.
- On mount (when open becomes true), focus the "Cancelar" button (more safe — prevents accidental Enter confirm). Use `useRef` + `useEffect([open])`.
- Tab trap: only "Cancelar" and "Confirmar" buttons reachable while open. Implement with `onKeyDown` on the panel that intercepts Tab to cycle between the two buttons only.
- Button classes from UI-SPEC Component 1:
  - Cancelar: secondary variant
  - Confirmar: danger variant
- If `isPending`, disable both buttons and change "Confirmar" label to pendingLabel (pass as optional prop, default to `confirmLabel + "..."`).
- Render `null` when `open` is false.
  </action>

  <verify>
    <automated>
      grep -q "aria-label=\"Mover para cima\"" src/components/admin/reorder-buttons.tsx
      grep -q "aria-label=\"Navegação\"" src/components/admin/breadcrumb.tsx
      grep -q "deriveCourseStatus" src/components/admin/status-badge.tsx
      grep -q "role=\"alertdialog\"" src/components/admin/confirmation-dialog.tsx
      npm run typecheck
      npm run lint
    </automated>
  </verify>

  <done>
    - Breadcrumb: Server Component, exports Breadcrumb, renders nav>ol with ChevronRight separators and aria-current="page" on last item
    - StatusBadge: exports StatusBadge and deriveCourseStatus, three status variants with exact Tailwind classes from UI-SPEC
    - ReorderButtons: "use client", two forms with separate server actions, disabled at boundaries, aria-disabled + disabled attrs, 44×44px touch targets
    - ConfirmationDialog: "use client", role="alertdialog", Escape closes, overlay click closes, focus on Cancelar on open, tab trap
    - npm run lint exits 0, npm run typecheck exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Course list page, redirect, and /admin/cursos/novo</name>
  <files>
    src/app/admin/page.tsx,
    src/app/admin/cursos/page.tsx,
    src/app/admin/cursos/novo/page.tsx
  </files>

  <read_first>
    1. .planning/phases/02-catalog-crud/02-PATTERNS.md — FIRST. Sections "src/app/admin/page.tsx (modify — redirect)" (line 669) and "Admin RSC Pages" (lines 607-666).
    2. .planning/phases/02-catalog-crud/02-UI-SPEC.md — Screen 1 (/admin/cursos layout, lines 381-438), Screen 2 heading structure. Copywriting Glossary for pt-BR strings.
    3. src/app/admin/page.tsx — read current file to understand what to replace.
    4. src/app/admin/course-manager.tsx — read to understand what form fields to reuse for /admin/cursos/novo.
    5. src/lib/courses/queries.ts — read to know what query exports to use (getAdminCourseList if added in 02-02, or getAvailableCourses for admin).
  </read_first>

  <action>
**A. Replace `src/app/admin/page.tsx`** with a redirect (FLAG-02 Opção A per D-05):

```typescript
import { redirect } from "next/navigation";

export const metadata = { title: "Admin — Gestão de Incidentes" };

export default function AdminPage() {
  redirect("/admin/cursos");
}
```

Remove all existing imports (CourseManager, auth checks — the redirect runs at render; /admin is protected by middleware which already requires admin role).

**B. Create `src/app/admin/cursos/page.tsx`** — RSC course list.

Pattern: `src/app/admin/page.tsx` for auth guard + layout shell. Replace `console.error` with `logger.error` per CLAUDE.md.

Auth guard (verbatim from PATTERNS.md lines 621-634): check session → redirect /login?redirectTo=/admin/cursos; check role → redirect /dashboard.

After auth, call `getAdminCourseList(supabase)` from `src/lib/courses/queries.ts` (added in plan 02-02 — returns all courses regardless of status).

Stats row calculation (RSC, inline):
- Count courses by `deriveCourseStatus(c.published_at, c.archived_at)` per course
- Build stats string parts: only include non-zero counts. e.g. `["3 publicados", "2 rascunhos"]`. Join with " · ".
- Render as `<p className="text-sm text-slate-500">{statsLine}</p>`

Page layout per UI-SPEC Screen 1:
- Header: `<header className="border-b border-slate-200 bg-white">` with "Gestão de Incidentes · Área restrita (admin)" and `<LogoutButton />`.
- Main: `mx-auto flex w-full max-w-6xl flex-1 flex-col gap-6 px-6 py-10`
- Eyebrow: `<p className="text-xs font-semibold uppercase tracking-[0.2em] text-slate-500">CATÁLOGO</p>`
- H1: `<h1 className="mt-2 text-2xl font-semibold text-slate-900">Catálogo de cursos</h1>`
- Subtitle: `<p className="mt-1 text-sm text-slate-600">Gerencie cursos publicados, rascunhos e arquivados.</p>`
- Stats row (if courses.length > 0)
- Button "Novo curso" (primary, link to /admin/cursos/novo): `<Link href="/admin/cursos/novo" className="...">`

Course list (space-y-3):
- Each course rendered as a card: `<article className={`flex items-center gap-4 rounded-2xl border border-slate-200 bg-white p-4 ${isArquivado ? "opacity-60" : ""}`}>`.
- Cover div 80×80: `<div className="h-20 w-20 shrink-0 rounded-xl overflow-hidden bg-slate-900">` with `<Image>` if cover_image_url, else placeholder.
- Title, slug, created_at formatted `dd/MM/yyyy`.
- `<StatusBadge status={deriveCourseStatus(course.published_at, course.archived_at)} />` from Task 1 component.
- published_at/archived_at metadata per UI-SPEC Component 13: `CalendarCheck`/`Archive` Lucide 14px + formatted date.
- Actions: "Editar" button (secondary, Link to `/admin/cursos/${course.slug}`) and "Arquivar" ghost button (only for non-archived courses, `text-amber-600 hover:text-amber-700`). The Arquivar button requires ConfirmationDialog — this is a client interaction. Since the page is RSC, extract just the archive button row into a small `"use client"` component `CourseArchiveButton` in the same file (or a separate client component if needed for tree-shaking). The archive dialog fires `archiveCourseAction` from 02-02 via form action.

Empty state per UI-SPEC: `BookOpen` icon 32px, "Nenhum curso cadastrado", "Crie o primeiro curso para começar.", "Novo curso" CTA.

**C. Create `src/app/admin/cursos/novo/page.tsx`** — RSC new course page (FLAG-01 Opção A).

Auth guard (same pattern). No data to fetch — blank form.

Layout:
- Eyebrow: CATÁLOGO
- H1: "Novo curso"
- Breadcrumb: `<Breadcrumb items={[{ label: "Catálogo", href: "/admin/cursos" }, { label: "Novo curso" }]} />`
- Card `bg-white rounded-2xl border p-6`:
  - Eyebrow: CURSO
  - H2: "Detalhes do curso"
  - Form renders the same fields as course-manager.tsx (title, slug, description, cover_image_url, certificate fields).
  - `action={createCourseAction}` from `src/app/actions/upsert-course.ts`.
  - Slug field includes helper text per UI-SPEC: "Use apenas letras minúsculas, números e hifens."
  - Client-side slugify preview: extract the slug suggestion logic into a small `"use client"` component `SlugPreview` that watches the title input via `onChange` on a controlled input and shows the slugified suggestion below the slug input. Import `slugify` from `src/lib/courses/slugify.ts`.
  - `FieldError` on slug field for `state.fieldErrors?.slug` (23505 collision per D-07).
  - FeedbackBanner per PATTERNS.md Shared Patterns section.
  - Submit button: "Salvar rascunho" (primary) using `useFormStatus()`.
  </action>

  <verify>
    <automated>
      grep -q "redirect.*admin\/cursos" src/app/admin/page.tsx
      grep -q "Catálogo de cursos" src/app/admin/cursos/page.tsx
      grep -q "Novo curso" src/app/admin/cursos/novo/page.tsx
      grep -q "Breadcrumb" src/app/admin/cursos/novo/page.tsx
      npm run typecheck
      npm run lint
    </automated>
  </verify>

  <done>
    - src/app/admin/page.tsx: only contains redirect("/admin/cursos") — no CourseManager import
    - /admin/cursos: renders h1 "Catálogo de cursos", stats row, course list with StatusBadge, Editar/Arquivar buttons, empty state
    - /admin/cursos/novo: renders Breadcrumb with "Catálogo" link, h1 "Novo curso", blank form with slug helper and slugify preview
    - npm run lint exits 0, npm run typecheck exits 0
  </done>
</task>

<task type="auto">
  <name>Task 3: Course/module/lesson edit pages (RSC)</name>
  <files>
    src/app/admin/cursos/[slug]/page.tsx,
    src/app/admin/cursos/[slug]/modulos/[moduleId]/page.tsx,
    src/app/admin/cursos/[slug]/aulas/[lessonId]/page.tsx,
    src/app/admin/course-manager.tsx
  </files>

  <read_first>
    1. .planning/phases/02-catalog-crud/02-PATTERNS.md — FIRST. Section "Admin RSC Pages" for params pattern (line 658), auth guard pattern (lines 621-634).
    2. .planning/phases/02-catalog-crud/02-UI-SPEC.md — Screen 2 (course edit, lines 440-511), Screen 3 (module edit, lines 513-557), Screen 4 (lesson edit, lines 559-624). Heading structure from Accessibility Notes (lines 893-918).
    3. src/app/admin/course-manager.tsx — read full file to extract reusable form field patterns before editing.
    4. src/lib/courses/queries.ts — check getCourseWithContent signature to know how to fetch course for edit page.
    5. src/lib/modules/schema.ts — check exports for updateModuleSchema.
    6. src/lib/lessons/schema.ts — check exports for updateLessonSchema.
  </read_first>

  <action>
**A. `src/app/admin/cursos/[slug]/page.tsx`** — Course edit + module list RSC.

```typescript
// Next.js 16 — params is a Promise; always await:
export default async function CourseEditPage({ params }: { params: Promise<{ slug: string }> }) {
  const { slug } = await params;
  // ... auth guard ...
}
```

After auth, fetch course by slug using `getCourseWithContent(slug, supabase)` or a new admin-scoped query from `src/lib/courses/queries.ts` (plan 02-02 adds admin query that returns all modules/lessons including deleted ones). If not found, `notFound()` from `next/navigation`.

Layout per UI-SPEC Screen 2:
- Breadcrumb: `<Breadcrumb items={[{ label: "Catálogo", href: "/admin/cursos" }, { label: course.title }]} />`
- H1: `{course.title}` (not "Detalhes do curso" — per Accessibility Notes).
- Section "Detalhes do curso" card (bg-white rounded-2xl border border-slate-200 p-6):
  - Eyebrow: CURSO
  - H2: "Detalhes do curso"
  - Form with `action={updateCourseAction}` (from 02-02). Hidden `<input name="course_id" value={course.id} />`.
  - Fields: title, slug (+ helper + SlugPreview client component), description, cover_image_url. Certificate subsection (bg-slate-50 rounded-xl border p-4) with certificate_title, certificate_description fields.
  - FeedbackBanner for `state.message`.
  - Action buttons row (`flex gap-3 flex-wrap items-center`):
    - "Salvar rascunho" — secondary, action `saveDraftAction`
    - "Publicar curso" — primary, action `publishCourseAction` — only rendered if `deriveCourseStatus(course.published_at, course.archived_at) === "rascunho"`
    - "Despublicar curso" — secondary, action `unpublishCourseAction` — only if status is "publicado"
    - "Arquivar curso" — ghost with `text-amber-600`, triggers ConfirmationDialog (copy: "Arquivar este curso?" / "O curso será removido da listagem dos alunos. Certificados e histórico de progresso já emitidos são preservados. Esta ação pode ser revertida via suporte." / "Arquivar curso" danger)
    - "Alunos com acesso" — ghost with Users icon 16px, Link to `/admin/cursos/${slug}/alunos`. Show enrollment count inline: "Alunos com acesso ({count})".

- Section "Módulos" card (bg-white rounded-2xl border border-slate-200 p-6):
  - Eyebrow: ESTRUTURA
  - H2: "Módulos"
  - "Adicionar módulo" button (primary small, opens inline form or Link to create — since we're RSC, render an inline collapsible "use client" component with a form pointing to `createModuleAction`).
  - Module list: for each module (sorted by `position`, filtered `deleted_at IS NULL` in query):
    - Card: `bg-slate-50 rounded-xl border border-slate-200 px-4 py-3 flex items-center gap-3`
    - `<ReorderButtons id={module.id} isFirst={index === 0} isLast={index === modules.length - 1} upAction={reorderModuleUpAction} downAction={reorderModuleDownAction} />` — pass reorder actions from 02-03.
    - Position number: `<span className="text-sm font-medium text-slate-500 w-6 shrink-0">{index + 1}</span>`
    - Title: `<span className="text-sm font-medium text-slate-900 flex-1">{module.title}</span>`
    - Lesson count: `<span className="text-xs text-slate-500 shrink-0">{lessonCount} aulas</span>`
    - Edit button: ghost, `<Link href={`/admin/cursos/${slug}/modulos/${module.id}`}>Editar</Link>`
  - Empty state: Layers icon, "Nenhum módulo criado", "Adicione módulos para organizar as aulas.", "Adicionar módulo" CTA.

**B. `src/app/admin/cursos/[slug]/modulos/[moduleId]/page.tsx`** — Module edit + lesson list RSC.

```typescript
export default async function ModuleEditPage({
  params,
}: {
  params: Promise<{ slug: string; moduleId: string }>;
}) {
  const { slug, moduleId } = await params;
  // ... auth guard ...
}
```

Fetch module by id + its lessons (filtered `deleted_at IS NULL`). If not found, `notFound()`.

Layout per UI-SPEC Screen 3:
- Breadcrumb: `<Breadcrumb items={[{ label: "Catálogo", href: "/admin/cursos" }, { label: courseTitle, href: `/admin/cursos/${slug}` }, { label: module.title }]} />`
- H1: `{module.title}`
- Section "Detalhes do módulo" card:
  - Eyebrow: MÓDULO; H2: "Detalhes do módulo"
  - Form `action={updateModuleAction}`. Hidden `<input name="module_id" value={module.id} />`, hidden `<input name="course_slug" value={slug} />`.
  - Fields: título * (required), descrição (textarea, optional), posição readonly (`<input readOnly value={module.position} ...>`).
  - FeedbackBanner.
  - Buttons: "Salvar módulo" (primary), "Remover módulo" (ghost, text-red-600, triggers ConfirmationDialog with copy "Remover este módulo?" / "Todas as aulas do módulo serão arquivadas. O histórico de progresso dos alunos é preservado. Use Restaurar aula se precisar desfazer." / "Remover módulo" danger, action `deleteModuleAction`).

- Section "Aulas do módulo" card:
  - Eyebrow: CONTEÚDO; H2: "Aulas do módulo"
  - "Adicionar aula" button (primary small, form pointing to `createLessonAction` with hidden module_id + course_slug).
  - Lesson list: for each lesson (sorted by position, deleted_at IS NULL):
    - Card with ReorderButtons (`reorderLessonUpAction`, `reorderLessonDownAction`)
    - Position number, title, workload display (`workload_minutes ? formatWorkload(minutes) : ""`)
    - Edit button: ghost, Link to `/admin/cursos/${slug}/aulas/${lesson.id}`
  - `formatWorkload(minutes)`: `minutes >= 60 ? `${Math.floor(minutes/60)}h ${minutes%60}min`.trim() : `${minutes} min``
  - Empty state: PlayCircle icon, "Nenhuma aula neste módulo", "Adicione a primeira aula deste módulo."

**C. `src/app/admin/cursos/[slug]/aulas/[lessonId]/page.tsx`** — Lesson edit + material section RSC.

```typescript
export default async function LessonEditPage({
  params,
}: {
  params: Promise<{ slug: string; lessonId: string }>;
}) {
  const { slug, lessonId } = await params;
```

Fetch lesson by id + its materials + parent module (for breadcrumb). If not found, `notFound()`.

Layout per UI-SPEC Screen 4:
- Breadcrumb: 4 levels: Catálogo → course title → module title → lesson title.
- H1: `{lesson.title}`
- If `lesson.deleted_at` is not null, render the "aula removida" amber banner (AlertTriangle icon, "Esta aula foi removida", `deleted_at` date, "Restaurar aula" button with `restoreLessonAction`).
- Section "Detalhes da aula" card:
  - Eyebrow: AULA; H2: "Detalhes da aula"
  - Grid 2 columns (md): título * + duração estimada (number input, min=1, name="workload_minutes").
  - Descrição textarea (optional).
  - Video subsection (bg-slate-50 rounded-xl border p-4):
    - Eyebrow: CONFIGURAÇÃO DE VÍDEO
    - Provider select: in production (`process.env.NODE_ENV === "production"`), render only `<option value="bunny">Bunny Stream</option>`. In non-prod, also render `<option value="youtube">YouTube (apenas dev)</option>`.
    - Helper text: "Em produção, use Bunny Stream. YouTube é apenas para desenvolvimento."
    - ID do vídeo input (text, placeholder="ID do vídeo no provider selecionado").
  - FeedbackBanner.
  - Buttons: "Salvar aula" (primary, `updateLessonAction`), "Remover aula" (ghost text-red-600, ConfirmationDialog copy "Remover esta aula?" / "A aula será arquivada. O histórico de progresso dos alunos que já a assistiram é preservado." / "Remover aula" danger).

- Section "Materiais da aula" card:
  - Eyebrow: MATERIAIS DE APOIO; H2: "Materiais da aula"
  - Material list (ul, divide-y divide-slate-100): MaterialListItem pattern from UI-SPEC Component 14 for each existing material. Trash2 icon ghost button fires `deleteMaterialAction`.
  - `<MaterialUpload lessonId={lesson.id} />` from plan 02-04 component.
  - Empty state: Paperclip icon, "Nenhum material anexado", "Faça upload de PDFs, planilhas ou imagens de apoio."

**D. Simplify `src/app/admin/course-manager.tsx`**

The original component handled both create and edit. After Phase 2, the edit logic has moved to `/admin/cursos/[slug]/page.tsx`. Update course-manager.tsx to:
- Remove the inline form if it was previously the primary entry point (now /novo handles it).
- Keep any utility types or sub-components that are imported by the new pages.
- Ensure no imports break (check what is still imported from course-manager.tsx by other files before deleting exports).
- If course-manager.tsx is no longer imported anywhere after this plan, replace its body with a comment noting it was superseded by /admin/cursos/page.tsx.
  </action>

  <verify>
    <automated>
      grep -q "Breadcrumb" src/app/admin/cursos/[slug]/page.tsx
      grep -q "await params" src/app/admin/cursos/[slug]/page.tsx
      grep -q "await params" "src/app/admin/cursos/[slug]/modulos/[moduleId]/page.tsx"
      grep -q "await params" "src/app/admin/cursos/[slug]/aulas/[lessonId]/page.tsx"
      grep -q "AlertTriangle" "src/app/admin/cursos/[slug]/aulas/[lessonId]/page.tsx"
      npm run typecheck
      npm run lint
    </automated>
  </verify>

  <done>
    - /admin/cursos/[slug]: renders Breadcrumb 2 levels, h1 with course title, Detalhes card + Módulos card with ReorderButtons, publish/archive action buttons
    - /admin/cursos/[slug]/modulos/[moduleId]: renders Breadcrumb 3 levels, h1 with module title, Detalhes + Aulas cards
    - /admin/cursos/[slug]/aulas/[lessonId]: renders Breadcrumb 4 levels, h1 with lesson title, deleted_at amber banner when applicable, video section with NODE_ENV-gated provider options, Materiais card
    - course-manager.tsx consistent (no broken imports)
    - npm run lint exits 0, npm run typecheck exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → RSC | URL params (slug, moduleId, lessonId) are untrusted; each page must validate entity existence via DB query and call notFound() on miss |
| Client Component → Server Action | Every action called from these pages already has requireAdminUser() guard (plans 02-02/03/04); UI layer does not re-implement auth |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | RSC admin pages — auth guard | mitigate | Each RSC page replicates the auth guard pattern from admin/page.tsx: check session → redirect /login; check role = "admin" → redirect /dashboard. Middleware pre-filters /admin/* routes but RSC guard is defense in depth. |
| T-05-02 | Information Disclosure | /admin/cursos/[slug] — course not found | mitigate | If getCourseBySlug returns null (slug enumeration by attacker), call `notFound()` which renders Next.js 404 page — does not reveal whether slug exists or is just unauthorized. |
| T-05-03 | Tampering | ConfirmationDialog — confirm button | accept | Dialog is client-side only; the actual destructive action is a Server Action with requireAdminUser() guard. A malicious actor bypassing the dialog still cannot execute without server-side auth. Low residual risk. |
</threat_model>

<verification>
After all three tasks:
- `npm run lint --max-warnings=0` exits 0
- `npm run typecheck` exits 0
- `npx vitest run` exits 0 (no new test files in this plan; existing tests must not regress)
- Manual smoke: `npm run dev`, navigate to /admin → auto-redirect to /admin/cursos; create course via /admin/cursos/novo; edit via /admin/cursos/[slug]; breadcrumb shows correct hierarchy on each nested page.
</verification>

<success_criteria>
- All 5 RSC pages exist and build without errors
- 4 shared components exist and are correctly typed
- /admin redirects to /admin/cursos (grep confirms redirect call)
- Breadcrumb renders on every page with /admin/cursos as root link
- StatusBadge derives status correctly from published_at/archived_at nulls
- ConfirmationDialog role="alertdialog", focus on Cancelar, Escape closes
- No TypeScript errors, no lint warnings
</success_criteria>

<output>
After completion, create `.planning/phases/02-catalog-crud/02-05-SUMMARY.md` following the summary template.
</output>
